# 言语言架构规划

## 一、 语言定位与设计理念

### 1.1 名称
**'言'** (Yán) — 取'语言''言说'之意，简洁有力，直指本质

### 1.2 核心哲学
- **文言为表，函数为里**：外观采用古典文言语法，内部基于纯函数式编程范式
- **极简主义**：没有括号、没有分号、没有关键字，只有动词与名词的流动
- **管道即自然**：数据如河流，动词如石激起涟漪

---

## 二、 语法设计

### 2.1 字法（词法）- 核心：无空格分词

**设计理念**：代码（动词/副词/标识符）之间无空格，数据之间才需要空格分隔。

#### 字符分类
| 类别 | 字符集 | 用途 |
|------|--------|------|
| `结构符` | `。` `，` `；` `''` `『』` | 语句结束、管道分隔、引用 |
| `数字` | `0-9` `.` `-` | 数值字面量，支持负数和小数 |
| `汉字` | CJK Unified Ideographs | 动词、标识符（即"代码"） |
| `西文` | `a-zA-Z0-9_` | 保留给 FFI 互操作 |

#### 分词规则（重点！）
```
输入：列1 2 3皆乘2
输出：列 | 1 | 2 | 3 | 皆 | 乘 | 2

规则：
1. 数字开头 → 切割为 NUM token
2. 汉字开头 → 切割为 WORD token（动词/副词）
3. 结构符（。 ， ' '）→ 立即切割
4. 空格 → 仅用于分隔 NUM 与 NUM，其他情况直接丢弃
```

#### 多字贪心匹配
```
优先级：3字 > 2字 > 1字
关键字表预加载，运行时可扩展

示例：定义阶乘 → 优先匹配'定义''阶乘'而非单字'定''义''阶''乘'
```

#### 注释与字符串
- 注释：`注` 开头至行尾，或 `--` 开头
- 字符串：`"..."` 包裹，允许空格：`"Hello World"` → 整体作为一个 STR token

### 2.2 句法（语法）

#### 核心结构
```ebnf
程序     = 语句*
语句     = 表达式 ('。' | '；')?
表达式   = 管道链
管道链   = 项 (',' 项)*    // ',' 为管道符，';' 可选显式结束
项      = 动词 参数*
动词    = 汉字+
参数    = 字面量 | 引用 | 嵌套管道
字面量  = 数字 | 字符串 | 列表 | 字典
引用    = ' 表达式 '
```

#### 语法示例（无空格风格）
```
列1 2 3皆乘2           // 列表 [1,2,3] 映射乘2 → [2,4,6]
10加5乘2               // (10+5)*2 → 30
若5大3则印"大"否则印"小"
定平方=函x乘x          // 定义函数
```

#### 核心区分机制：元数（Arity）驱动

**问题**：代码和数据混杂在一起，如何区分哪些是动词、哪些是数据？

**答案**：不是靠"类型标签"区分，而是靠**元数驱动**——动词声明自己需要几个参数，参数收集够了就开始新的动词。

```
列1 2 3皆乘2
│ │ │ │  │ │
│ │ │ │  │ └─ 数据（乘的右参数：2）
│ │ │ │  └──── 动词（乘：arity=2，需要左+右两个参数）
│ │ │ └─────── 副词（皆：arity=2，修饰下一个动词）
│ │ └───────── 数据（列的第二个参数：2）
│ └─────────── 数据（列的第一个参数：1）
└──────────── 动词（列：arity=-1，可变参数）
```

##### 规则
| 角色 | 判断依据 | 示例 |
|------|----------|------|
| **动词** | 在关键字表中，且需要收集参数 | `列`、`乘`、`加` |
| **副词** | 在副词表中，修饰下一个动词 | `皆`、`只`、`归` |
| **数据** | 数字、字符串、引号内容 | `1`、`2`、`『hello』` |

##### 收集逻辑
```
1. 遇到动词 → 创建新的 Call 节点，开始收集参数
2. 继续读取 token，根据动词的元数决定收集策略：
   - arity=2：收集2个数据后，当前 Call 结束
   - arity=-1：持续收集直到遇到下一个动词
3. 遇到新动词 → 前一个 Call 完成，压入 Pipeline
```

##### 示例推演：`列1 2 3皆乘2`
```
1. 读'列'→ 创建 Call(verb=列, args=[])
2. 读'1'→ 列需要参数，加入 args=[1]
3. 读'2'→ 列需要参数，加入 args=[1,2]
4. 读'3'→ 列需要参数，加入 args=[1,2,3]
5. 读'皆'→ 列的元数=-1，遇到新动词，列 Call 完成，压入 Pipeline
6. 读'乘'→ 创建 Call(verb=乘, args=[])
7. 读'2'→ 乘需要2个参数，加入 args=[2]
8. 乘的元数已满，遇到'。'→ 乘 Call 完成，压入 Pipeline
9. 后处理：副词'皆'吞噬右侧的 Call(乘)，形成 Call(皆, [Call(乘)])
```

这就是**文言编程的核心魔法**——无需空格分隔，依赖元数自动推断边界。

### 2.2.1 核心语法规则（最简设计）

```
┌─────────────────────────────────────────────────────────────┐
│ 1. 句号'。'求值                                             │
│    每遇到句号，当前管道链执行，返回结果                        │
├─────────────────────────────────────────────────────────────┤
│ 2. 逗号'，'管道                                             │
│    逗号前的求值结果，作为逗号后操作的第一个隐式参数            │
│    例：10加5，乘2 → 先算10+5=15 → 再算15*2=30                │
├─────────────────────────────────────────────────────────────┤
│ 3. 动词-宾语：向右吞噬                                        │
│    动词(操作符)主动向右收集数据，直到遇到：                    │
│    - 另一个动词                                               │
│    - 逗号'，'                                               │
│    - 句号'。'                                               │
├─────────────────────────────────────────────────────────────┤
│ 4. 偏函数应用（Partial Application）                          │
│    如果动词只吞噬了部分参数，不立即求值，                      │
│    而是变成'待执行函数'，等待管道传来的数据                  │
│    例：乘2 → 变成 λx. x*2，等待管道传入 x                     │
└─────────────────────────────────────────────────────────────┘
```

#### 语法示例

```
10加5，乘2。           → (10+5)*2 = 30
列1 2 3，皆乘2。        → [1,2,3].map(x => x*2) = [2,4,6]
5，乘2。               → 5 * 2 = 10 （显式管道）
5乘2。                 → 5 * 2 = 10 （隐式直接求值）
```

##### 隐式直接求值（无管道符）

当动词**左右两侧都有数据**时，无需逗号，直接组合求值：
```
5乘2。   → 乘(5, 2) = 10
10加5。  → 加(10, 5) = 15
```

**求值逻辑**：
1. 动词收集右侧数据
2. 检查动词左侧是否有遗留数据
3. 如果有，自动补全为完整调用 `verb(left, right)`

### 2.3 词类（语义）

#### 动词分类
| 类型 | 特征 | 示例 |
|------|------|------|
| **原语** | 内置不可重定义 | `加`、`乘`、`列`、`印` |
| **函数** | 用户定义 | `平方`、`阶乘` |
| **宏** | 编译时展开 | `若`、`定` |
| **特化符** | 改变动词行为 | `皆`（映射）、`只`（过滤） |

#### 特化符（副词）系统
```
皆  -> map      映射变换
只  -> filter  过滤保留
归  -> reduce  归约聚合
潜  -> flatmap 扁平映射
```

---

## 三、 架构设计

### 3.1 编译管道

```
┌─────────────┐    ┌─────────────┐    ┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│   源码      │───▶│   词法      │───▶│   语法      │───▶│   语义      │───▶│   解释/编译 │
│   文本      │    │   Token流   │    │   AST       │    │   核心AST   │    │   目标代码  │
└─────────────┘    └─────────────┘    └─────────────┘    └─────────────┘    └─────────────┘
```

### 3.2 模块划分

#### 模块 1：词法分析器 (lexer.py)
```python
class Lexer:
    - tokenize(source: str) -> List[Token]
    - 多字关键字贪心匹配
    - 字符串/注释逃逸处理
```

#### 模块 2：语法分析器 (parser.py)
```python
class Parser:
    - parse(tokens: List[Token]) -> AST
    - 管道链构建 (Pipeline)
    - 引用处理 (Quote)
    - 嵌套表达式解析
```

#### 模块 3：语义分析器 (semantic.py)
```python
class SemanticAnalyzer:
    - 类型检查
    - 作用域分析
    - 闭包分析
    - 尾递归检测
```

#### 模块 4：求值器/编译器 (eval.py / codegen.py)
```python
class Evaluator:
    - evaluate(ast: AST, env: Env) -> Any
    
class CodeGenerator:
    - generate(ast: AST) -> str  # 输出 Python/JS/Wasm
```

#### 模块 5：标准库 (stdlib/)
```
- 数据构造：列、典、序、对
- 算术运算：加、减、乘、除、模、幂
- 比较运算：大、小、等于、不等于
- 逻辑运算：且、或、非
- 高阶函数：皆、只、归、潜
- 控制流：若、则、否则、循环
- I/O：印、读、写
```

### 3.3 核心数据结构

#### AST 节点定义
```python
@dataclass
class Node:
    """基类"""
    loc: Location  # 源码位置

@dataclass
class Num(Node):
    value: float | int

@dataclass
class Str(Node):
    value: str

@dataclass
class List(Node):
    elements: List[Node]

@dataclass
class Dict(Node):
    pairs: Dict[Node, Node]

@dataclass
class Word(Node):
    name: str

@dataclass
class Verb(Node):
    name: str
    args: List[Node]
    is_partial: bool  # 是否为偏函数

@dataclass
class Pipeline(Node):
    steps: List[Verb]  # 管道步骤序列

@dataclass
class Quote(Node):
    expr: Node  # 未求值的 AST

@dataclass
class If(Node):
    cond: Node
    then: Node
    else_: Node

@dataclass
class Define(Node):
    name: Word
    value: Node
```

#### 环境定义
```python
@dataclass
class Binding:
    value: Any
    arity: int          # 元数：期望参数数量，-1 表示可变
    is_macro: bool      # 是否为宏
    is_builtin: bool    # 是否为内置

class Env:
    bindings: Dict[str, Binding]
    parent: Env | None
    
    def lookup(name: str) -> Binding
    def define(name: str, binding: Binding)
    def extend() -> Env  # 新建子作用域
```

---

## 四、 关键特性设计

### 4.1 柯里化与管道注入

#### 严格柯里化规则
```python
# 动词参数不足时，返回偏函数
def apply_verb(verb, args, pipeline_value):
    arity = get_arity(verb)
    provided = len(args) + (1 if pipeline_value else 0)
    
    if provided < arity:
        # 返回闭包，捕获已提供的参数
        return partial(verb, *args, pipeline_value)
    elif provided == arity:
        # 参数已满，执行
        return verb(*args, pipeline_value)
    else:
        # 参数溢出，错误
        raise ArityError(f"动词 {verb} 需要 {arity} 参数，提供了 {provided}")
```

#### 管道注入规则
```
Pipeline 中每个 Verb：
1. 若 pipeline_value 为空 → 正常求值
2. 若 pipeline_value 非空：
   - Verb 是闭包（偏函数）→ 将 pipeline_value 作为首参数
   - Verb 是可变参数(arity=-1) → 追加到参数列表
   - Verb 参数已满 → 管道溢出错误
```

### 4.2 引用、同像性与元编程

**核心思想**：代码即数据，数据即代码。AST 本身就是一等公民。

```python
# ' ' 创建一个 Quote 节点，内部 AST 不求值
# 源代码：1加2  → AST：Verb("加", [Num(1), Num(2)])
Quote(expr=Verb("加", [Num(1), Num(2)]))

# 行 动词强制求值 AST
行 '1加2'  # → 3
```

#### 数据 ↔ 代码 转换

| 操作 | 语法 | 说明 |
|------|------|------|
| **引用 (Quote)** | `'expr` | 将表达式转为 AST 数据，不求值 |
| **求值 (Eval)** | 行 ast | 将 AST 数据作为代码执行 |
| **解引用 (Unquote)** | `,expr` | 在 Quote 内部插入动态内容 |

#### 示例：自编程能力

```python
# 构建动态代码
定 代码 = '列1 2 3皆乘2

# 修改 AST
定 加5 = 函 x 加 x 5
定 新代码 = 变换 代码  // 在 AST 中替换节点

# 执行动态代码
行 新代码  # → [6, 7, 8]
```

#### 宏系统

基于 Quote/Eval，可以实现 Lisp 级别的宏：
- 接收 AST → 变换 AST → 返回新 AST
- 编译时展开，无需运行时开销

```python
# 定义宏：交换参数
定 交换 = 宏 'a加b' 'b加a'

# 使用宏
交换 3加5  # 展开为 5加3 → 8
```

### 4.3 尾递归优化

```python
# 检测尾递归调用，自动转换为迭代
def optimize_tail_recursion(ast):
    # 识别尾递归模式
    # 转换为 while 循环
    return optimized_ast
```

### 4.4 惰性求值

```python
# 延迟计算直到真正需要
缓 = 函 x 印 x  # 定义时不执行
缓 "hello"      # 调用时执行
```

---

## 五、 实现规划

### 阶段 1：MVP（核心解释器）✅
- [x] 词法分析：多字关键字匹配、无空格分词
- [x] 语法分析：管道构建、副词处理、中缀动词
- [x] Python 转译器：代码生成、柯里化、管道注入
- [x] 运行时支持：算术、比较、逻辑、列表、高阶函数
- [x] 标准库基础：加、减、乘、除、列、印、皆、只、归
- [x] 变量定义：定 名称 = 值
- [x] 函数定义：定 名称 = 函 参数... 体
- [x] 条件语句：若...则...否则...

### 阶段 2：函数式增强 ✅
- [x] 列表操作：皆、只、归
- [x] 条件分支：若、则、否则
- [x] 函数定义：定、函
- [x] 递归函数支持（两遍解析识别用户定义函数）
- [ ] 尾递归优化

### 阶段 3：同像性
- [ ] 引用机制：`'expr` Quote
- [ ] 求值机制：行Eval
- [ ] 宏系统

### 阶段 4：扩展
- [ ] 目标代码生成：Python、JavaScript、WebAssembly
- [ ] REPL 交互式环境完善
- [ ] 调试器/Profiler
- [ ] 标准库完善

---

## 六、 关键技术决策

### 6.1 实现语言
- **首选**：Python（快速原型，易于调试）
- **备选**：Rust（生产级性能）

### 6.2 目标平台
- **本地**：Python 解释器直接运行
- **Web**：WebAssembly 编译
- **互操作**：支持调用 Python/JS 原生函数

### 6.3 错误处理
- 源码位置追踪（文件名、行号、列号）
- 友好的错误信息（显示上下文）
- 恢复机制（尽可能继续解析）

---

## 七、 文件结构

```
yanya/
├── lexer.py         # 词法分析器
├── parser.py        # 语法分析器
├── semantic.py      # 语义分析器
├── eval.py          # 解释器
├── codegen.py       # 代码生成器
├── ast.py           # AST 节点定义
├── env.py           # 环境与作用域
├── stdlib/
│   ├── __init__.py
│   ├── data.py      # 数据构造
│   ├── math.py      # 数学运算
│   ├── highorder.py # 高阶函数
│   └── control.py   # 控制流
├── utils/
│   ├── location.py  # 源码位置
│   └── error.py     # 错误处理
├── repl.py          # 交互式环境
└── main.py          # 入口
```

---

## 八、 命名规范（对照表）

| 中文动词 | 英文对应 | 语义 |
|----------|----------|------|
| 列 | list | 构造列表 |
| 典 | dict | 构造字典 |
| 序 | tuple | 构造元组 |
| 加 | add | 加法 |
| 减 | sub | 减法 |
| 乘 | mul | 乘法 |
| 除 | div | 除法 |
| 模 | mod | 取模 |
| 幂 | pow | 幂运算 |
| 大 | gt | 大于 |
| 小 | lt | 小于 |
| 等 | eq | 等于 |
| 且 | and | 逻辑与 |
| 或 | or | 逻辑或 |
| 非 | not | 逻辑非 |
| 皆 | map | 映射 |
| 只 | filter | 过滤 |
| 归 | reduce | 归约 |
| 潜 | flatmap | 扁平映射 |
| 首 | head | 首元素 |
| 余 | tail | 尾列表 |
| 入 | nth | 索引取值 |
| 印 | print | 打印 |
| 若 | if | 条件 |
| 则 | then | 真分支 |
| 否则 | else | 假分支 |
| 定 | define | 定义 |
| 函 | lambda | 匿名函数 |
| 行 | eval | 求值 |
| `'` | quote | 引用 |
| `,` | unquote | 解引用 |