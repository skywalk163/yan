#!/usr/bin/env python3
"""修复 text_process 示例"""

import re

# 读取文件
with open(r'G:\dumategithub\newlisp\yan\playground\index.html', 'r', encoding='utf-8') as f:
    content = f.read()

# 替换整个 text_process 示例
old_text = '''            text_process: `-- 文本处理示例
-- 展示言语言的字符串操作能力

印 "=== 文本处理示例 ==="
印 ""

-- 1. 基本字符串操作
印 "--- 1. 基本字符串操作 ---"

定义 文本 = "Hello, 言语言!"
印 连 "原始文本: " 文本

-- 字符串长度
定义 长度值 = 长度 文本
印 连 "长度: " 长度值

-- 转换大小写
定义 小写文本 = 小写 文本
印 连 "小写: " 小写文本

定义 大写文本 = 大写 文本
印 "--- 2. 查找与检测 ---"。

-- 查找子串位置
定 位置 = 查找 去空后 "言语言"。
印 连 "\\"言语言\\" 的位置: " 位置。

-- 检测包含
定 包含结果 = 包含 去空后 "文本"。
印 连 "包含 \\"文本\\": " 包含结果。

-- 检测开头
定 开头结果 = 开头是 去空后 "Hello"。
印 连 "开头是 \\"Hello\\": " 开头结果。

-- 检测结尾
定 结尾结果 = 结尾是 去空后 "示例"。
印 连 "结尾是 \\"示例\\": " 结尾结果。

印 ""。

-- 3. 字符串替换
印 "--- 3. 字符串替换 ---"。

定 模板 = "你好，{名字}！欢迎来到{地点}。"。
印 连 "模板: " 模板。

-- 替换名字
定 步骤1 = 替换 模板 "{名字}" "张三"。
印 连 "替换名字: " 步骤1。

-- 替换地点
定 步骤2 = 替换 步骤1 "{地点}" "北京"。
印 连 "替换地点: " 步骤2。

印 ""。
印 "=== 文本处理完成 ==="。`,'''

new_text = '''            text_process: `-- 文本处理示例
-- 展示言语言的字符串操作能力

印 "=== 文本处理示例 ==="
印 ""

-- 1. 基本字符串操作
印 "--- 1. 基本字符串操作 ---"

定义 文本 = "Hello, 言语言!"
印 连 "原始文本: " 文本

-- 字符串长度
定义 长度值 = 长度 文本
印 连 "长度: " 长度值

-- 转换大小写
定义 小写文本 = 小写 文本
印 连 "小写: " 小写文本

定义 大写文本 = 大写 文本
印 连 "大写: " 大写文本

-- 2. 字符串替换
印 "--- 2. 字符串替换 ---"

定义 模板 = "你好，言语言！欢迎来到编程世界！"
定义 替换后 = 替换 模板 "言语言" "新世界"
印 连 "替换后: " 替换后

印 "=== 文本处理完成 ==="`,'''

if old_text in content:
    content = content.replace(old_text, new_text)
    with open(r'G:\dumategithub\newlisp\yan\playground\index.html', 'w', encoding='utf-8') as f:
        f.write(content)
    print("✅ text_process 示例已修复")
else:
    print("❌ 未找到旧的 text_process 示例")
