# 内置函数扩展计划

## 当前内置函数清单

### 数学库 ✅
- 三角函数：正弦、余弦、正切、反正弦、反余弦、反正切
- 指数对数：指数、对数、对数10
- 其他：开方、取整、进位、四舍五入、随机、随机整数、圆周率、自然常数

### 字符串库 ✅
- 基础：长度、连接、分割、替换、截取
- 大小写：小写、大写
- 查找：查找、包含、去空、开头是、结尾是

### 文件库 ✅
- 读写：读文件、写文件、追加文件
- 检查：存在、是文件、是目录
- 操作：列目录、建目录、删文件、删目录
- 路径：当前目录、文件名、目录名、扩展名

### 时间库 ✅
- 获取：当前时间、日期、时间、日期时间
- 格式化：格式化时间
- 工具：睡眠

### 类型检查 ✅
- 是数、是串、是表、是函、是真、是空、类型

### 列表操作 ✅
- 基础：首、余、入、长、添、连、含、空
- 高阶：皆（map）、只（filter）、归（reduce）

## 待添加的内置函数

### 优先级1：常用工具函数
1. **列表操作**
   - `反` - 反转列表
   - `排` - 排序列表
   - `最大` - 最大值
   - `最小` - 最小值
   - `求和` - 求和
   - `计数` - 计数元素出现次数

2. **字典操作**
   - `键` - 获取所有键
   - `值` - 获取所有值
   - `项` - 获取所有键值对
   - `删键` - 删除键

3. **数学工具**
   - `符号` - 符号函数（返回-1, 0, 1）
   - `最大公约数` - GCD
   - `最小公倍数` - LCM
   - `绝对值` - 绝对值（已有`绝对`）

### 优先级2：高级功能
1. **JSON处理**
   - `编码JSON` - 对象转JSON字符串
   - `解码JSON` - JSON字符串转对象

2. **正则表达式**
   - `匹配` - 正则匹配
   - `替换全部` - 正则替换

3. **网络工具**
   - `HTTP获取` - HTTP GET请求
   - `HTTP提交` - HTTP POST请求

### 优先级3：系统工具
1. **进程管理**
   - `执行` - 执行系统命令
   - `环境变量` - 获取环境变量

2. **编码转换**
   - `编码` - 字符串编码
   - `解码` - 字节解码

## 实施计划

### 阶段1：优先级1函数（1-2天）
- 实现列表操作函数
- 实现字典操作函数
- 编写测试用例

### 阶段2：优先级2函数（2-3天）
- 实现JSON处理
- 实现正则表达式
- 编写测试用例

### 阶段3：优先级3函数（1-2天）
- 实现系统工具
- 编写测试用例

## 实现原则

1. **命名一致性**：使用简洁的中文单字或双字
2. **参数一致性**：遵循现有动词的参数模式
3. **文档完善**：每个函数都有清晰的文档
4. **测试覆盖**：每个函数都有测试用例

---

*创建时间：2026-05-16*
