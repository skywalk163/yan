# 语法分析器 模块 API 文档

---

## 函数: `get_global_user_verbs()`

**行号**: 22

### 描述

> 获取全局用户动词集合

## 函数: `add_global_user_verb(name)`

**行号**: 27

### 描述

> 添加全局用户动词

## 函数: `clear_global_user_verbs()`

**行号**: 32

### 描述

> 清空全局用户动词集合

## 类: `BlockStack`

**行号**: 37

### 描述

> 块栈，用于跟踪当前块的状态

### 方法

#### `__init__()`

**行号**: 40


#### `push(block_type, indent_level)`

**行号**: 43

> 压入新块

#### `pop()`

**行号**: 50

> 弹出块，返回弹出的块信息，如果栈为空则返回 None

#### `current()`

**行号**: 56

> 获取当前块，如果栈为空则返回 None

#### `depth()`

**行号**: 62

> 获取栈深度

#### `is_empty()`

**行号**: 66

> 检查栈是否为空

#### `current_indent()`

**行号**: 70

> 获取当前缩进级别

## 类: `Parser`

**行号**: 77

### 描述

> 语法分析器

### 方法

#### `__init__(use_global_verbs)`

**行号**: 168


#### `VERBS()`

**行号**: 177

> 合并内置动词和用户定义动词

#### `parse(tokens)`

**行号**: 183


#### `_collect_user_verbs()`

**行号**: 209

> 第一遍扫描：收集所有用户定义的函数名及其参数数量

#### `_current()`

**行号**: 249


#### `_peek(offset)`

**行号**: 252


#### `_is_at_end()`

**行号**: 258


#### `_advance()`

**行号**: 261


#### `_match()`

**行号**: 267


#### `_expect(token_type, message)`

**行号**: 273


#### `_check_word(value)`

**行号**: 278


#### `__init__(use_global_verbs)`

**行号**: 282


#### `VERBS()`

**行号**: 293

> 合并内置动词和用户定义动词

#### `_reset_verbs_cache()`

**行号**: 302

> 重置动词缓存（在收集新用户动词后调用）

#### `_is_verb(name)`

**行号**: 306

> 优化：直接使用集合查找

#### `_try_match_user_verb()`

**行号**: 310

> 尝试匹配用户定义的函数名（可能被拆分为多个 WORD 或与其他汉字组合）

#### `_collect_call_args()`

**行号**: 377

> 收集函数调用的参数

#### `_parse_statement(consume_dot)`

**行号**: 393


#### `_parse_define()`

**行号**: 426


#### `_parse_lambda()`

**行号**: 469


#### `_parse_block()`

**行号**: 517

> 解析代码块（支持缩进语法）

#### `_get_current_indent()`

**行号**: 582

> 获取当前缩进级别

#### `_get_peek_indent()`

**行号**: 586

> 查看下一个缩进级别（通过查看 INDENT/DEDENT token）

#### `_should_end_block(block_start_indent)`

**行号**: 601

> 判断是否应该结束当前块
> 
> 参数：
>     block_start_indent: 块开始时的缩进级别
> 
> 返回：
>     True 如果应该结束块，False 否则

#### `_is_block_end()`

**行号**: 637

> 检查是否到达块结束

#### `_parse_expression()`

**行号**: 666


#### `_parse_pipeline()`

**行号**: 669


#### `_parse_expr()`

**行号**: 679

> 解析表达式：动词 数据* (副词 动词 数据*)*

#### `_parse_term()`

**行号**: 729

> 解析项

#### `_parse_atom()`

**行号**: 977

> 解析原子

#### `_parse_if()`

**行号**: 1057

> 解析条件语句：若 条件 [则]：分支。否则：分支。
> 
> 支持两种语法：
> 1. 若 条件 则：分支。否则：分支。（传统语法）
> 2. 若 条件：分支。否则：分支。（省略则，更简洁）
> 3. 若 条件 换行缩进 分支（新缩进语法）

#### `_parse_block_until(stop_words, always_block)`

**行号**: 1130

> 解析块，直到遇到指定的停止词
> 
> 参数：
>     stop_words: 停止词集合
>     always_block: 如果为 True，即使只有一个语句也返回 Block

#### `_parse_foreach()`

**行号**: 1202

> 解析遍历循环：遍历 变量 于 列表：循环体。
> 
> 支持两种语法：
> 1. 遍历 变量 于 列表：循环体。（传统语法）
> 2. 遍历 变量 于 列表 换行缩进 循环体（新缩进语法）

#### `_parse_iterable()`

**行号**: 1234

> 解析可迭代对象，处理可能被拆分的标识符

#### `_parse_while()`

**行号**: 1262

> 解析当循环：当 条件 则：循环体 或 当 条件：循环体。
> 
> 支持的语法：
> 1. 当 条件：循环体。（传统语法）
> 2. 当 条件 则：循环体。（传统语法）
> 3. 当 变量 中缀动词 值：循环体。（如 当 小 j 边界：）
> 4. 当 条件 换行缩进 循环体（新缩进语法）

#### `_parse_expr_until(stop_words)`

**行号**: 1290

> 解析表达式，直到遇到指定的停止词
> 
> stop_words 可以包含字符串（WORD 值）和特殊标记如 '：'（冒号）和 'INDENT'（缩进）

#### `_parse_test()`

**行号**: 1326

> 解析测试：测 "测试名"：测试体。

#### `_parse_test_suite()`

**行号**: 1345

> 解析测试套件：套 "套件名"：测试列表。

#### `_parse_import()`

**行号**: 1407

> 解析导入语句：导入 模块名 或 导入 名称1 名称2 于 模块名

#### `_parse_export()`

**行号**: 1438

> 解析导出语句：导出 名称1 名称2 ...

#### `_parse_struct_def()`

**行号**: 1457

> 解析结构体定义：结构 名称 字段1 类型1 字段2 类型2 ...

## 函数: `push(self, block_type, indent_level)`

**行号**: 43

### 描述

> 压入新块

## 函数: `pop(self)`

**行号**: 50

### 描述

> 弹出块，返回弹出的块信息，如果栈为空则返回 None

## 函数: `current(self)`

**行号**: 56

### 描述

> 获取当前块，如果栈为空则返回 None

## 函数: `depth(self)`

**行号**: 62

### 描述

> 获取栈深度

## 函数: `is_empty(self)`

**行号**: 66

### 描述

> 检查栈是否为空

## 函数: `current_indent(self)`

**行号**: 70

### 描述

> 获取当前缩进级别

## 函数: `VERBS(self)`

**行号**: 177

### 描述

> 合并内置动词和用户定义动词

## 函数: `_collect_user_verbs(self)`

**行号**: 209

### 描述

> 第一遍扫描：收集所有用户定义的函数名及其参数数量

## 函数: `VERBS(self)`

**行号**: 293

### 描述

> 合并内置动词和用户定义动词

## 函数: `_reset_verbs_cache(self)`

**行号**: 302

### 描述

> 重置动词缓存（在收集新用户动词后调用）

## 函数: `_is_verb(self, name)`

**行号**: 306

### 描述

> 优化：直接使用集合查找

## 函数: `_try_match_user_verb(self)`

**行号**: 310

### 描述

> 尝试匹配用户定义的函数名（可能被拆分为多个 WORD 或与其他汉字组合）

## 函数: `_collect_call_args(self)`

**行号**: 377

### 描述

> 收集函数调用的参数

## 函数: `_parse_block(self)`

**行号**: 517

### 描述

> 解析代码块（支持缩进语法）

## 函数: `_get_current_indent(self)`

**行号**: 582

### 描述

> 获取当前缩进级别

## 函数: `_get_peek_indent(self)`

**行号**: 586

### 描述

> 查看下一个缩进级别（通过查看 INDENT/DEDENT token）

## 函数: `_should_end_block(self, block_start_indent)`

**行号**: 601

### 描述

> 判断是否应该结束当前块
> 
> 参数：
>     block_start_indent: 块开始时的缩进级别
> 
> 返回：
>     True 如果应该结束块，False 否则

## 函数: `_is_block_end(self)`

**行号**: 637

### 描述

> 检查是否到达块结束

## 函数: `_parse_expr(self)`

**行号**: 679

### 描述

> 解析表达式：动词 数据* (副词 动词 数据*)*

## 函数: `_parse_term(self)`

**行号**: 729

### 描述

> 解析项

## 函数: `_parse_atom(self)`

**行号**: 977

### 描述

> 解析原子

## 函数: `_parse_if(self)`

**行号**: 1057

### 描述

> 解析条件语句：若 条件 [则]：分支。否则：分支。
> 
> 支持两种语法：
> 1. 若 条件 则：分支。否则：分支。（传统语法）
> 2. 若 条件：分支。否则：分支。（省略则，更简洁）
> 3. 若 条件 换行缩进 分支（新缩进语法）

## 函数: `_parse_block_until(self, stop_words, always_block)`

**行号**: 1130

### 描述

> 解析块，直到遇到指定的停止词
> 
> 参数：
>     stop_words: 停止词集合
>     always_block: 如果为 True，即使只有一个语句也返回 Block

## 函数: `_parse_foreach(self)`

**行号**: 1202

### 描述

> 解析遍历循环：遍历 变量 于 列表：循环体。
> 
> 支持两种语法：
> 1. 遍历 变量 于 列表：循环体。（传统语法）
> 2. 遍历 变量 于 列表 换行缩进 循环体（新缩进语法）

## 函数: `_parse_iterable(self)`

**行号**: 1234

### 描述

> 解析可迭代对象，处理可能被拆分的标识符

## 函数: `_parse_while(self)`

**行号**: 1262

### 描述

> 解析当循环：当 条件 则：循环体 或 当 条件：循环体。
> 
> 支持的语法：
> 1. 当 条件：循环体。（传统语法）
> 2. 当 条件 则：循环体。（传统语法）
> 3. 当 变量 中缀动词 值：循环体。（如 当 小 j 边界：）
> 4. 当 条件 换行缩进 循环体（新缩进语法）

## 函数: `_parse_expr_until(self, stop_words)`

**行号**: 1290

### 描述

> 解析表达式，直到遇到指定的停止词
> 
> stop_words 可以包含字符串（WORD 值）和特殊标记如 '：'（冒号）和 'INDENT'（缩进）

## 函数: `_parse_test(self)`

**行号**: 1326

### 描述

> 解析测试：测 "测试名"：测试体。

## 函数: `_parse_test_suite(self)`

**行号**: 1345

### 描述

> 解析测试套件：套 "套件名"：测试列表。

## 函数: `_parse_import(self)`

**行号**: 1407

### 描述

> 解析导入语句：导入 模块名 或 导入 名称1 名称2 于 模块名

## 函数: `_parse_export(self)`

**行号**: 1438

### 描述

> 解析导出语句：导出 名称1 名称2 ...

## 函数: `_parse_struct_def(self)`

**行号**: 1457

### 描述

> 解析结构体定义：结构 名称 字段1 类型1 字段2 类型2 ...
