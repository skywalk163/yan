# 言语言入门教程 - 第5章：综合实战

## 5.1 项目1：计算器

```yan
注 简单计算器

定 计算 = 函 表达式
  {{
import re
tokens = re.findall(r'(\d+\.?\d*|[+\-*/])', 表达式)
result = float(tokens[0])
for i in range(1, len(tokens), 2):
    op = tokens[i]
    num = float(tokens[i+1])
    if op == '+':
        result += num
    elif op == '-':
        result -= num
    elif op == '*':
        result *= num
    elif op == '/':
        result /= num
result
}}。

印 "简单计算器"。
印 "输入表达式（如：1+2*3）："。
定 输入 = 读。
定 结果 = 计算 输入。
印 "结果："连结果。
```

## 5.2 项目2：待办事项管理器

```yan
注 待办事项管理器

定 待办列表 = 列。

定 添加待办 = 函 事项
  定 待办列表 = 添 待办列表 事项
  印 "已添加："连事项

定 删除待办 = 函 索引
  若 索引大等于0且索引小长待办列表
    则
      定 已删除 = 待办列表[索引]
      定 待办列表 = 连 待办列表[:索引] 待办列表[索引加1:]
      印 "已删除："连已删除
    否则
      印 "无效索引"

定 显示待办 = 函
  若 长待办列表等0
    则 印 "待办列表为空"
    否则
      印 "待办列表："
      遍历 i 于 范围长待办列表：
        印 i连". "连待办列表[i]

注 测试
添加待办 "学习言语言"。
添加待办 "完成项目"。
添加待办 "写文档"。
显示待办。
删除待办 1。
显示待办。
```

## 5.3 项目3：斐波那契数列

```yan
注 斐波那契数列

定 斐波那契 = 函 n
  {{
if n < 2:
    return n
return 斐波那契(n-1) + 斐波那契(n-2)
}}。

定 n = 10。
印 "斐波那契数列前"连n连"项："。
遍历 i 于 范围n：
  印 斐波那契i
```

## 5.4 项目4：汉诺塔

```yan
注 汉诺塔问题

定 汉诺塔 = 函 盘子数 源 目标 辅助
  若 盘子数大0
    则
      汉诺塔 盘子数减1 源 辅助 目标
      印 "移动盘子"连盘子数"从"连源"到"连目标
      汉诺塔 盘子数减1 辅助 目标 源

汉诺塔 3 "A" "C" "B"。
```

## 5.5 项目5：词频统计

```yan
注 词频统计

定 统计词频 = 函 文本
  定 词频 = 典
  定 单词 = {{import re; re.findall(r'[\w]+', 文本.lower())}}
  遍历 词 于 单词
    若 含 键词频 词
      则 定 词频[词] = 词频[词]加1
      否则 定 词频[词] = 1
  返词频

定 文本 = "Hello world hello Python world"。
定 结果 = 统计词频 文本。
印 结果。
```

## 5.6 项目6：温度转换器

```yan
注 温度转换器

定 华氏转摄氏 = 函 华氏度
  返 华氏度减32乘5除9

定 摄氏转华氏 = 函 摄氏度
  返 摄氏度乘9除5加32

印 "温度转换器"。
印 "1. 华氏转摄氏"。
印 "2. 摄氏转华氏"。
定 选择 = 读。

若 选择等"1"
  则
    印 "输入华氏度："
    定 华氏 = {{float(input())}}
    印 华氏连"°F = "连华氏转摄氏华氏连"°C"
  否则若 选择等"2"
    则
      印 "输入摄氏度："
      定 摄氏 = {{float(input())}}
      印 摄氏连"°C = "连摄氏转华氏摄氏连"°F"
  否则 印 "无效选择"
```

## 5.7 项目7：简单Web服务器

```yan
注 简单Web服务器

定 启动服务器 = 函 端口
  {{
import http.server
import socketserver
Handler = http.server.SimpleHTTPRequestHandler
with socketserver.TCPServer(("", 端口), Handler) as httpd:
    print(f"服务器运行在端口 {端口}")
    httpd.serve_forever()
}}。

启动服务器 8000。
```

## 5.8 项目8：JSON数据处理

```yan
注 JSON数据处理示例

引 JSON。

定 数据 = JSON.解析 "{\"users\": [{\"name\": \"张三\", \"age\": 25}, {\"name\": \"李四\", \"age\": 30}]}"。

印 "所有用户："。
遍历 用户 于 数据["users"]
  印 用户["name"]连"，年龄："连用户["age"]

定 成年用户 = 只 函u u["age"]大等于18 数据["users"]。
印 "\n成年用户："。
印 成年用户。
```

## 5.9 项目9：猜数字游戏

```yan
注 猜数字游戏

定 猜数字 = 函
  定 答案 = {{import random; random.randint(1, 100)}}
  定 尝试次数 = 0
  
  印 "猜数字游戏！"
  印 "我想了一个1-100之间的数字。"
  
  当 真
    印 "请输入你的猜测："
    定 猜测 = {{int(input())}}
    定 尝试次数 = 尝试次数加1
    
    若 猜测等答案
      则
        印 "恭喜！你猜对了！用了"连尝试次数连"次。"
        返
      否则若 猜测小答案
        则 印 "太小了！"
        否则 印 "太大了！"

猜数字。
```

## 5.10 项目10：倒计时器

```yan
注 倒计时器

定 倒计时 = 函 秒数
  遍历 i 于 范围秒数：
    定 剩余 = 秒数减i
    印 剩余连"..."
    {{import time; time.sleep(1)}}
  印 "时间到！"

倒计时 5。
```

## 5.11 小结

本章介绍了10个实用的言语言示例项目：
- 计算器
- 待办事项管理器
- 斐波那契数列
- 汉诺塔
- 词频统计
- 温度转换器
- Web服务器
- JSON数据处理
- 猜数字游戏
- 倒计时器

通过这些项目，你可以掌握言语言的核心用法。继续探索，创造更多有趣的应用！