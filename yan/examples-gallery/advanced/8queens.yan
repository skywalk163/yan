注 八皇后问题 - 回溯算法经典案例
注 难度：⭐⭐⭐⭐⭐
注 学习目标：理解回溯算法和约束满足问题

印 "=== 八皇后问题示例 ==="。

注 ========== 问题说明 ==========
印 "\n========== 问题说明 ==========="。

印 "八皇后问题："。
印 "在8×8的国际象棋棋盘上放置8个皇后"。
印 "使得任意两个皇后都不能互相攻击"。
印 "即：同行、同列、同对角线上不能有两个皇后"。

注 ========== 回溯算法实现 ==========
印 "\n========== 回溯算法实现 ==========="。

定 解的数量 = 0。
定 当前解 = 列。

注 检查位置是否安全
定 是安全的 = 函 行 列 皇后的位置
  {{
    # 检查是否与已放置的皇后冲突
    for i in range(len(皇后的位置)):
        # 检查列冲突
        if 皇后的位置[i] == 列:
            return False
        # 检查对角线冲突
        if abs(皇后的位置[i] - 列) == abs(i - 行):
            return False
    return True
  }}。
。

注 回溯求解
定 求解N皇后 = 函 n 行 皇后的位置
  若 等 行 n
  则
    定 解的数量 = 加 解的数量 1。
    印 "解"连解的数量连"："连皇后的位置。
  。
  否则
    定 尝试列 = 函 列
      若 等 列 n
      则 0。
      否则
        若 是安全的 行 列 皇后的位置
        则
          定 新位置 = 连 皇后的位置 列 列。
          求解N皇后 n 加 行 1 新位置。
        。
        尝试列 加 列 1。
      。
    。
    尝试列 0。
  。
。

印 "\n求解八皇后问题（显示前10个解）："。
求解N皇后 8 0 列。
印 "\n总解数："连解的数量。

注 ========== 可视化显示 ==========
印 "\n========== 可视化显示 ==========="。

定 显示棋盘 = 函 皇后的位置
  {{
    n = len(皇后的位置)
    print("\n  " + " ".join([str(i) for i in range(n)]))
    print("  " + "-" * (2 * n - 1))
    
    for row in range(n):
        line = f"{row}|"
        for col in range(n):
            if 皇后的位置[row] == col:
                line += "Q "
            else:
                line += ". "
        print(line)
  }}。
。

印 "\n第一个解的棋盘："。
定 第一个解 = 列 0 4 7 5 2 6 1 3。
显示棋盘 第一个解。

注 ========== 优化版本 ==========
印 "\n========== 优化版本 ==========="。

印 "使用位运算优化（更快）："。

定 快速求解 = 函 n
  {{
    def solve(row, cols, diag1, diag2, positions):
        if row == n:
            return [positions[:]]
        
        solutions = []
        # 计算可用位置
        available = ((1 << n) - 1) & ~(cols | diag1 | diag2)
        
        while available:
            # 选择最右边的可用位置
            pos = available & (-available)
            col = (pos - 1).bit_length()
            
            positions.append(col)
            solutions.extend(solve(
                row + 1,
                cols | pos,
                (diag1 | pos) << 1,
                (diag2 | pos) >> 1,
                positions
            ))
            positions.pop()
            
            available &= available - 1
        
        return solutions
    
    return solve(0, 0, 0, 0, [])
  }}。
。

定 所有解 = 快速求解 8。
印 "使用位运算找到"连长 所有解连"个解"。

注 ========== N皇后问题 ==========
印 "\n========== N皇后问题 ==========="。

印 "不同棋盘大小的解数："。

定 计算解数 = 函 n
  {{
    # 预计算的解数（OEIS A000170）
    known_solutions = {
        1: 1, 2: 0, 3: 0, 4: 2, 5: 10,
        6: 4, 7: 40, 8: 92, 9: 352, 10: 724
    }
    return known_solutions.get(n, "?")
  }}。
。

定 显示解数 = 函 n
  若 大 n 10
  则 0。
  否则
    定 解数 = 计算解数 n。
    印 n连"×"连n连"棋盘："连解数连"个解"。
    显示解数 加 n 1。
  。
。

显示解数 1。

注 ========== 算法分析 ==========
印 "\n========== 算法分析 ==========="。

印 "回溯算法特点："。
印 "1. 时间复杂度：O(n!) - 指数级"。
印 "2. 空间复杂度：O(n) - 递归栈深度"。
印 "3. 剪枝优化：提前排除不安全的位置"。
印 "4. 位运算优化：使用位掩码加速判断"。

注 ========== 实际应用 ==========
印 "\n========== 实际应用 ==========="。

印 "八皇后问题的实际意义："。
印 "1. 算法教学 - 回溯算法的经典案例"。
印 "2. 约束满足 - 资源分配、调度问题"。
印 "3. 组合优化 - 寻找所有可行解"。
印 "4. 并行计算 - 不同分支可并行求解"。

注 ========== 扩展问题 ==========
印 "\n========== 扩展问题 ==========="。

印 "相关扩展问题："。
印 "1. N皇后问题 - 推广到N×N棋盘"。
印 "2. N皇后计数 - 只计算解的数量"。
印 "3. N皇后构造 - 构造一个解"。
印 "4. 超级皇后 - 皇后还能像马一样移动"。

印 "\n八皇后问题示例完成！"。
印 "共找到92个不同的解。"。
