注 斐波那契数列 - 多种实现方式对比
注 难度：⭐⭐⭐⭐
注 学习目标：理解不同算法实现的性能差异

印 "=== 斐波那契数列示例 ==="。

注 ========== 递归实现 ==========
印 "\n========== 递归实现 ==========="。

定 斐波那契递归 = 函 n
  若 小 n 2
  则 n
  否则 加 斐波那契递归 减 n 1 斐波那契递归 减 n 2

印 "递归实现（简单但效率低）："。
印 "fib(10) = "连斐波那契递归 10。

印 "前15个斐波那契数："。
定 打印前N个 = 函 n
  若 小 n 0
  则 0
  否则
    印 斐波那契递归 n
    打印前N个 减 n 1
打印前N个 14。

注 ========== 尾递归优化 ==========
印 "\n========== 尾递归优化 ==========="。

定 斐波那契尾递归 = 函 n a b
  若 等 n 0
  则 a
  否则 斐波那契尾递归 减 n 1 b 加 a b

印 "尾递归实现（优化性能）："。
印 "fib(10) = "连斐波那契尾递归 10 0 1。
印 "fib(20) = "连斐波那契尾递归 20 0 1。
印 "fib(30) = "连斐波那契尾递归 30 0 1。

注 ========== 迭代实现 ==========
印 "\n========== 迭代实现 ==========="。

定 斐波那契迭代 = 函 n
  若 小 n 2
  则 n
  否则
    定 计算 = 函 a b 计数
      若 等 计数 n
      则 b
      否则 计算 b 加 a b 加 计数 1
    计算 0 1 1

印 "迭代实现（最高效）："。
印 "fib(10) = "连斐波那契迭代 10。
印 "fib(20) = "连斐波那契迭代 20。
印 "fib(30) = "连斐波那契迭代 30。

注 ========== 生成斐波那契数列 ==========
印 "\n========== 生成斐波那契数列 ==========="。

定 生成斐波那契数列 = 函 n
  定 生成 = 函 列表 a b 计数
    若 等 计数 n
    则 列表
    否则
      定 新列表 = 入 b 列表
      生成 新列表 b 加 a b 加 计数 1
  
  若 小 n 1
  则 列
  否则 生成 列 1 0 1

印 "生成前10个斐波那契数："。
定 数列 = 生成斐波那契数列 10。
印 数列。

注 ========== 黄金分割比验证 ==========
印 "\n========== 黄金分割比验证 ==========="。

印 "验证斐波那契数列趋近黄金分割比（φ ≈ 1.618）："。

定 验证黄金比 = 函 n
  定 fib_n = 斐波那契迭代 n
  定 fib_n_minus_1 = 斐波那契迭代 减 n 1
  定 比值 = 除 fib_n fib_n_minus_1
  
  印 "fib("连n连") / fib("连减 n 1连") = "连比值

验证黄金比 10。
验证黄金比 20。
验证黄金比 30。

注 ========== 矩阵快速幂实现 ==========
印 "\n========== 矩阵快速幂实现 ==========="。

印 "使用矩阵快速幂计算（理论最优）："。

定 矩阵乘法 = 函 A B
  {{
    # 2x2矩阵乘法
    return [
        [A[0][0]*B[0][0] + A[0][1]*B[1][0], A[0][0]*B[0][1] + A[0][1]*B[1][1]],
        [A[1][0]*B[0][0] + A[1][1]*B[1][0], A[1][0]*B[0][1] + A[1][1]*B[1][1]]
    ]
  }}

定 矩阵快速幂 = 函 M n
  {{
    # 矩阵快速幂
    if n == 1:
        return M
    if n % 2 == 0:
        half = 矩阵快速幂(M, n // 2)
        return 矩阵乘法(half, half)
    else:
        return 矩阵乘法(M, 矩阵快速幂(M, n - 1))
  }}

定 斐波那契矩阵 = 函 n
  若 等 n 0
  则 0
  否则
    定 基础矩阵 = 列 列 1 1 列 1 0
    定 结果矩阵 = 矩阵快速幂 基础矩阵 n
    首 首 结果矩阵

印 "fib(10) = "连斐波那契矩阵 10。
印 "fib(20) = "连斐波那契矩阵 20。

注 ========== 性能对比 ==========
印 "\n========== 性能对比 ==========="。

印 "算法复杂度对比："。
印 "1. 递归实现：O(2^n) - 指数级，极慢"。
印 "2. 尾递归优化：O(n) - 线性，较快"。
印 "3. 迭代实现：O(n) - 线性，最快"。
印 "4. 矩阵快速幂：O(log n) - 对数级，理论最优"。

印 "\n推荐使用：迭代实现或尾递归优化"。

注 ========== 实用应用 ==========
印 "\n========== 实用应用 ==========="。

印 "斐波那契数列的应用："。
印 "1. 算法教学 - 递归与迭代的经典案例"。
印 "2. 黄金分割 - 艺术和设计中的比例"。
印 "3. 自然现象 - 植物叶序、花瓣排列"。
印 "4. 金融分析 - 斐波那契回调线"。

注 示例：计算黄金矩形
印 "\n黄金矩形示例："。

定 黄金比例 = 1.618。
定 短边 = 100。
定 长边 = 乘 短边 黄金比例。

印 "短边："连短边。
印 "长边："连长边。
印 "比例："连除 长边 短边。
