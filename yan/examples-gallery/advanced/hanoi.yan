注 汉诺塔 - 经典递归问题
注 难度：⭐⭐⭐⭐
注 学习目标：理解递归思维和问题分解

印 "=== 汉诺塔示例 ==="。

注 ========== 基本实现 ==========
印 "\n========== 基本实现 ==========="。

定 移动次数 = 0。

定 汉诺塔 = 函 n 源柱 辅助柱 目标柱
  若 等 n 1
  则
    印 源柱连" → "连目标柱。
    定 移动次数 = 加 移动次数 1。
  。
  否则
    汉诺塔 减 n 1 源柱 目标柱 辅助柱。
    印 源柱连" → "连目标柱。
    定 移动次数 = 加 移动次数 1。
    汉诺塔 减 n 1 辅助柱 源柱 目标柱。
  。
。

印 "汉诺塔（3个盘子）："。
汉诺塔 3 "A" "B" "C"。
印 "总移动次数："连移动次数。

注 ========== 可视化版本 ==========
印 "\n========== 可视化版本 ==========="。

定 创建柱子 = 函 n
  {{
    # 创建初始状态：n个盘子在源柱
    return {
        'A': list(range(n, 0, -1)),  # 从大到小
        'B': [],
        'C': []
    }
  }}。
。

定 显示状态 = 函 柱子
  {{
    print(f"A: {柱子['A']}")
    print(f"B: {柱子['B']}")
    print(f"C: {柱子['C']}")
    print()
  }}。
。

定 汉诺塔可视化 = 函 n 柱子 源柱 辅助柱 目标柱
  若 等 n 1
  则
    {{
      # 移动盘子
      disk = 柱子[源柱].pop()
      柱子[目标柱].append(disk)
      print(f"移动盘子 {disk} 从 {源柱} 到 {目标柱}")
    }}。
    显示状态 柱子。
  。
  否则
    汉诺塔可视化 减 n 1 柱子 源柱 目标柱 辅助柱。
    {{
      disk = 柱子[源柱].pop()
      柱子[目标柱].append(disk)
      print(f"移动盘子 {disk} 从 {源柱} 到 {目标柱}")
    }}。
    显示状态 柱子。
    汉诺塔可视化 减 n 1 柱子 辅助柱 源柱 目标柱。
  。
。

印 "\n汉诺塔可视化（3个盘子）："。
定 初始柱子 = 创建柱子 3。
印 "初始状态："。
显示状态 初始柱子。

汉诺塔可视化 3 初始柱子 "A" "B" "C"。

注 ========== 步骤计数 ==========
印 "\n========== 步骤计数 ==========="。

定 计算最少步数 = 函 n
  减 幂 2 n 1。
。

印 "汉诺塔最少移动次数公式：2^n - 1"。
印 "\n不同盘子数量的最少步数："。

定 显示步数 = 函 n
  若 大 n 10
  则 0。
  否则
    定 步数 = 计算最少步数 n。
    印 n连"个盘子："连步数连"步"。
    显示步数 加 n 1。
  。
。

显示步数 1。

注 ========== 递归深度分析 ==========
印 "\n========== 递归深度分析 ==========="。

印 "汉诺塔的递归特点："。
印 "1. 问题分解：将n个盘子的问题分解为n-1个盘子的问题"。
印 "2. 递归关系：T(n) = 2*T(n-1) + 1"。
印 "3. 时间复杂度：O(2^n)"。
印 "4. 空间复杂度：O(n) - 递归栈深度"。

注 ========== 变体问题 ==========
印 "\n========== 变体问题 ==========="。

注 三柱汉诺塔（标准）
印 "标准汉诺塔：三根柱子"。

注 四柱汉诺塔
印 "\n四柱汉诺塔（Frame-Stewart算法）："。

定 四柱汉诺塔步数 = 函 n
  {{
    # Frame-Stewart算法估算
    # 实际最优解仍是开放问题
        return float('inf')
    
    # 简化估算
    import math
    return int(n * math.sqrt(2 * n))
  }}。
。

印 "4个盘子（三柱）："连计算最少步数 4连"步"。
印 "4个盘子（四柱）：约"连四柱汉诺塔步数 4连"步"。

注 ========== 实际应用 ==========
印 "\n========== 实际应用 ==========="。

印 "汉诺塔问题的实际意义："。
印 "1. 算法教学 - 递归思维的经典案例"。
印 "2. 问题分解 - 将复杂问题分解为子问题"。
印 "3. 优化思考 - 寻找最优解"。
印 "4. 编程练习 - 理解递归和栈"。

注 ========== 性能优化 ==========
印 "\n========== 性能优化 ==========="。

印 "优化建议："。
印 "1. 尾递归优化 - 减少栈空间"。
印 "2. 记忆化 - 避免重复计算（汉诺塔不适用）"。
印 "3. 迭代实现 - 使用显式栈"。

注 迭代实现（使用栈）
印 "\n迭代实现（非递归）："。

定 汉诺塔迭代 = 函 n
  {{
    # 使用栈模拟递归
    stack = [(n, 'A', 'B', 'C', False)]
    moves = []
    
    while stack:
        disks, source, auxiliary, target, processed = stack.pop()
        
        if disks == 1:
            moves.append(f"{source} → {target}")
        elif processed:
            moves.append(f"{source} → {target}")
        else:
            # 模拟递归调用
            stack.append((disks - 1, auxiliary, source, target, False))
            stack.append((1, source, auxiliary, target, True))
            stack.append((disks - 1, source, target, auxiliary, False))
    
    return moves
  }}。
。

定 移动步骤 = 汉诺塔迭代 3。
印 "3个盘子的移动步骤："。
遍历 步骤 于 移动步骤：
  印 步骤。
。

印 "\n汉诺塔示例完成！"。
