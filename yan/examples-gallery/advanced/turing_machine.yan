注 图灵机 - 计算理论模拟
注 难度：⭐⭐⭐⭐⭐
注 学习目标：理解图灵机的计算模型

印 "=== 图灵机示例 ==="。

注 ========== 图灵机定义 ==========
印 "\n========== 图灵机定义 ==========="。

印 "图灵机组成："。
印 "1. 无限长纸带 - 分成格子，每个格子一个符号"。
印 "2. 读写头 - 可以读取和写入符号"。
印 "3. 状态寄存器 - 存储当前状态"。
印 "4. 转移函数 - 根据当前状态和符号决定动作"。

注 ========== 简单图灵机实现 ==========
印 "\n========== 简单图灵机实现 ==========="。

定 创建图灵机 = 函 初始纸带 初始状态 转移规则
  典
    "纸带" 初始纸带
    "位置" 0
    "状态" 初始状态
    "规则" 转移规则
    "步骤" 0

定 执行步骤 = 函 机器
  {{
    纸带 = 机器['纸带']
    位置 = 机器['位置']
    状态 = 机器['状态']
    规则 = 机器['规则']
    
    # 读取当前符号
    if 位置 < 0:
        纸带 = ['_'] * abs(位置) + 纸带
        位置 = 0
    elif 位置 >= len(纸带):
        纸带 = 纸带 + ['_'] * (位置 - len(纸带) + 1)
    
    当前符号 = 纸带[位置]
    
    # 查找转移规则
    规则键 = (状态, 当前符号)
    if 规则键 not in 规则:
        return None  # 停机
    
    新状态, 写入符号, 移动方向 = 规则[规则键]
    
    # 执行动作
    纸带[位置] = 写入符号
    if 移动方向 == 'R':
        位置 += 1
    elif 移动方向 == 'L':
        位置 -= 1
    
    return {
        '纸带': 纸带,
        '位置': 位置,
        '状态': 新状态,
        '规则': 规则,
        '步骤': 机器['步骤'] + 1
    }
  }}

定 运行图灵机 = 函 机器 最大步数
  {{
    for i in range(最大步数):
        if 机器 is None:
            return None
        
        # 显示当前状态
        纸带 = 机器['纸带']
        位置 = 机器['位置']
        状态 = 机器['状态']
        
        # 构造可视化
        可视化 = ""
        for j in range(len(纸带)):
            if j == 位置:
                可视化 += f"[{纸带[j]}]"
            else:
                可视化 += f" {纸带[j]} "
        
        print(f"步骤{机器['步骤']:3d}: 状态={状态:5s} {可视化}")
        
        机器 = 执行步骤(机器)
    
    return 机器
  }}

注 ========== 示例1：二进制加1 ==========
印 "\n========== 示例1：二进制加1 ==========="。

印 "图灵机：二进制数加1"。

定 加1规则 = 典
  "q0,0" 列 "q0" "0" "R"
  "q0,1" 列 "q0" "1" "R"
  "q0,_" 列 "q1" "_" "L"
  "q1,0" 列 "q2" "1" "R"
  "q1,1" 列 "q1" "0" "L"
  "q1,_" 列 "q2" "1" "R"

定 机器1 = 创建图灵机 列 "1" "0" "1" "1" "q0" 加1规则。
印 "\n输入：1011（二进制）"。
印 "执行过程："。
运行图灵机 机器1 20。

注 ========== 示例2：回文检测 ==========
印 "\n========== 示例2：回文检测 ==========="。

印 "图灵机：检测字符串是否为回文"。

定 回文规则 = 典
  "q0,0" 列 "q1" "_" "R"
  "q0,1" 列 "q2" "_" "R"
  "q0,_" 列 "accept" "_" "R"
  "q1,0" 列 "q1" "0" "R"
  "q1,1" 列 "q1" "1" "R"
  "q1,_" 列 "q3" "_" "L"
  "q3,0" 列 "q4" "_" "R"
  "q3,_" 列 "accept" "_" "R"

印 "\n输入：101（回文）"。
定 机器2 = 创建图灵机 列 "1" "0" "1" "q0" 回文规则。
运行图灵机 机器2 20。

注 ========== 示例3：复制字符串 ==========
印 "\n========== 示例3：复制字符串 ==========="。

印 "图灵机：复制字符串"。

定 复制规则 = 典
  "q0,0" 列 "q1" "X" "R"
  "q0,1" 列 "q2" "Y" "R"
  "q0,_" 列 "qf" "_" "R"

印 "\n输入：101"。
定 机器3 = 创建图灵机 列 "1" "0" "1" "q0" 复制规则。
运行图灵机 机器3 20。

注 ========== 通用图灵机 ==========
印 "\n========== 通用图灵机 ==========="。

印 "通用图灵机（UTM）："。
印 "可以模拟任何其他图灵机的图灵机"。
印 "是现代计算机的理论基础"。
印 "证明了：任何可计算的问题都可以用图灵机解决"。

注 ========== 图灵完备性 ==========
印 "\n========== 图灵完备性 ==========="。

印 "图灵完备性："。
印 "一个计算系统如果能模拟通用图灵机，则称其图灵完备"。
印 "\n图灵完备的系统："。
印 "1. 现代编程语言（Python、C、Java等）"。
印 "2. Lambda演算"。
印 "3. 细胞自动机（如Conway生命游戏）"。
印 "4. 言语言 ✅"。

注 ========== 计算复杂性 ==========
印 "\n========== 计算复杂性 ==========="。

印 "计算复杂性分类："。
印 "1. P类问题 - 多项式时间可解"。
印 "2. NP类问题 - 多项式时间可验证"。
印 "3. NP完全问题 - NP中最难的问题"。
印 "4. 不可判定问题 - 图灵机无法解决"。

注 ========== 停机问题 ==========
印 "\n========== 停机问题 ==========="。

印 "停机问题（不可判定）："。
印 "问题：给定程序和输入，判断程序是否会停机"。
印 "结论：不存在通用算法能解决停机问题"。
印 "证明：通过自指悖论（类似"这句话是假的"）"。

注 ========== 实际意义 ==========
印 "\n========== 实际意义 ==========="。

印 "图灵机的实际意义："。
印 "1. 计算理论基础 - 定义什么是可计算的"。
印 "2. 计算复杂性理论 - P vs NP问题"。
印 "3. 编程语言理论 - 图灵完备性"。
印 "4. 人工智能 - 计算的极限"。

印 "\n图灵机示例完成！"。
