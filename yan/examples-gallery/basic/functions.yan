注 函数定义 - 函数定义和调用
注 难度：⭐⭐
注 学习目标：掌握函数的定义、调用和参数传递

印 "=== 函数定义示例 ==="。

注 无参数函数
定 打招呼 = 函
  印 "你好！"。
。

打招呼。

注 单参数函数
定 平方 = 函 x
  乘 x x。
。

印 "5的平方："连平方 5。
印 "10的平方："连平方 10。

注 多参数函数
定 相加 = 函 a b
  加 a b。
。

印 "3 + 4 = "连相加 3 4。
印 "100 + 200 = "连相加 100 200。

注 函数返回值
定 最大值 = 函 a b
  若 大 a b
  则 a。
  否则 b。
。

印 "max(10, 20) = "连最大值 10 20。
印 "max(30, 15) = "连最大值 30 15。

注 函数组合
定 两倍 = 函 x
  乘 x 2。
。

定 加十 = 函 x
  加 x 10。
。

定 结果1 = 两倍 5。
印 "两倍(5) = "连结果1。

定 结果2 = 加十 结果1。
印 "加十(两倍(5)) = "连结果2。

注 递归函数
定 阶乘 = 函 n
  若 等 n 0
  则 1。
  否则 乘 n 阶乘 减 n 1。
。

印 "5! = "连阶乘 5。
印 "10! = "连阶乘 10。

注 高阶函数（函数作为参数）
定 应用函数 = 函 f x
  f x。
。

印 "应用平方函数到7："连应用函数 平方 7。
印 "应用两倍函数到7："连应用函数 两倍 7。

注 函数作为返回值
定 创建加法器 = 函 n
  函 x
    加 x n。
。

定 加五 = 创建加法器 5。
印 "加五(10) = "连加五 10。
印 "加五(20) = "连加五 20。

注 实用函数示例
定 是偶数 = 函 n
  等 余 n 2 0。
。

印 "10是偶数："连是偶数 10。
印 "7是偶数："连是偶数 7。

定 绝对值 = 函 n
  若 小 n 0
  则 减 0 n。
  否则 n。
。

印 "abs(-5) = "连绝对值 -5。
印 "abs(5) = "连绝对值 5。
