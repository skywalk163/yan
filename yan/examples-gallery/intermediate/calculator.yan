注 计算器 - 表达式解析和计算
注 难度：⭐⭐⭐⭐
注 学习目标：实现一个支持四则运算的计算器

印 "=== 计算器示例 ==="。

注 ========== 简单计算器 ==========
印 "\n========== 简单计算器 ==========="。

注 基本运算函数
定 加法 = 函 a b 加 a b。
定 减法 = 函 a b 减 a b。
定 乘法 = 函 a b 乘 a b。
定 除法 = 函 a b 除 a b。

印 "基本运算："。
印 "5 + 3 = "连加法 5 3。
印 "10 - 4 = "连减法 10 4。
印 "6 × 7 = "连乘法 6 7。
印 "20 ÷ 4 = "连除法 20 4。

注 ========== 表达式计算器 ==========
印 "\n========== 表达式计算器 ==========="。

注 解析简单表达式（支持加减乘除）
定 解析表达式 = 函 表达式
  注 这里使用Python的正则表达式进行解析
  {{
    import re
    
    # 提取数字和运算符
    tokens = re.findall(r'(\d+\.?\d*|[+\-*/])', 表达式)
    
    if not tokens:
        return 0
    
    # 先处理乘除
    i = 1
    while i < len(tokens):
        if tokens[i] in ['*', '/']:
            left = float(tokens[i-1])
            right = float(tokens[i+1])
            if tokens[i] == '*':
                result = left * right
            else:
                result = left / right
            tokens[i-1:i+2] = [str(result)]
        else:
            i += 2
    
    # 再处理加减
    result = float(tokens[0])
    i = 1
    while i < len(tokens):
        op = tokens[i]
        num = float(tokens[i+1])
        if op == '+':
            result += num
        elif op == '-':
            result -= num
        i += 2
    
    return result
  }}

印 "\n表达式计算："。

定 表达式1 = "10 + 5"。
印 表达式1连" = "连解析表达式 表达式1。

定 表达式2 = "20 - 8"。
印 表达式2连" = "连解析表达式 表达式2。

定 表达式3 = "6 * 7"。
印 表达式3连" = "连解析表达式 表达式3。

定 表达式4 = "100 / 4"。
印 表达式4连" = "连解析表达式 表达式4。

注 复合表达式
印 "\n复合表达式："。

定 表达式5 = "10 + 20 * 3"。
印 表达式5连" = "连解析表达式 表达式5。

定 表达式6 = "100 - 50 / 2"。
印 表达式6连" = "连解析表达式 表达式6。

注 ========== 科学计算器 ==========
印 "\n========== 科学计算器 ==========="。

导入 数学。

注 幂运算
定 幂运算 = 函 base exp 幂 base exp。

注 开方运算
定 开方运算 = 函 x 开方 x。

注 三角函数
定 正弦运算 = 函 x 正弦 x。
定 余弦运算 = 函 x 余弦 x。

印 "\n科学计算："。
印 "2^10 = "连幂运算 2 10。
印 "sqrt(144) = "连开方运算 144。
印 "sin(π/2) = "连正弦运算 除 圆周率 2。
印 "cos(0) = "连余弦运算 0。

注 ========== 单位转换器 ==========
印 "\n========== 单位转换器 ==========="。

定 摄氏转华氏 = 函 celsius
  加 乘 celsius 除 9 5 32

定 华氏转摄氏 = 函 fahrenheit
  乘 减 fahrenheit 32 除 5 9

定 千米转英里 = 函 km
  乘 km 0.621371

印 "\n单位转换："。
印 "25°C = "连摄氏转华氏 25连"°F"。
印 "77°F = "连华氏转摄氏 77连"°C"。
印 "100km = "连千米转英里 100连"英里"。

注 ========== 百分比计算器 ==========
印 "\n========== 百分比计算器 ==========="。

定 计算百分比 = 函 部分 总计
  乘 除 部分 总计 100

定 增加百分比 = 函 数值 百分比
  乘 数值 加 1 除 百分比 100

定 减少百分比 = 函 数值 百分比
  乘 数值 减 1 除 百分比 100

印 "\n百分比计算："。
印 "25是200的："连计算百分比 25 200连"%"。
印 "100增加20%："连增加百分比 100 20。
印 "100减少20%："连减少百分比 100 20。

注 ========== 实用计算器 ==========
印 "\n========== 实用计算器 ==========="。

注 BMI计算器
印 "\nBMI计算器："。

定 计算BMI = 函 体重 身高
  除 体重 幂 身高 2

定 体重 = 70。
定 身高 = 1.75。
定 BMI = 计算BMI 体重 身高。

印 "体重："连体重连"kg"。
印 "身高："连身高连"m"。
印 "BMI："连BMI。

若 小 BMI 18.5
则 印 "体重过轻"。
否则 若 小 BMI 24
  则 印 "正常体重"
  否则 若 小 BMI 28
    则 印 "超重"
    否则 印 "肥胖"

注 利息计算器
印 "\n利息计算器："。

定 计算简单利息 = 函 本金 利率 年数
  乘 本金 乘 利率 年数

定 计算复利 = 函 本金 利率 年数
  乘 本金 幂 加 1 利率 年数

定 本金 = 10000。
定 年利率 = 0.05。
定 年数 = 3。

印 "本金："连本金连"元"。
印 "年利率："连乘 年利率 100连"%"。
印 "年数："连年数连"年"。
印 "简单利息："连计算简单利息 本金 年利率 年数连"元"。
印 "复利总额："连计算复利 本金 年利率 年数连"元"。

注 ========== 交互式计算器 ==========
印 "\n========== 交互式计算器 ==========="。

印 "欢迎使用言语言计算器！"。
印 "支持运算：+ - * / ^ sqrt sin cos"。
印 "示例：10 + 20 * 3"。

注 实际使用时可以取消注释
注 印 "请输入表达式："。
注 定 用户输入 = 读。
注 定 计算结果 = 解析表达式 用户输入。
注 印 "结果："连计算结果。
