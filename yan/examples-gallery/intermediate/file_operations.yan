注 文件操作 - 文件读写和目录操作
注 难度：⭐⭐⭐
注 学习目标：掌握文件和目录的基本操作

印 "=== 文件操作示例 ==="。

注 ========== 写入文件 ==========
印 "\n========== 写入文件 ==========="。

定 文件名 = "test_file.txt"。
定 内容 = "这是言语言写入的文件内容\n第二行\n第三行"。

印 "写入文件："连文件名。
写文件 文件名 内容。
印 "写入成功！"。

注 ========== 读取文件 ==========
印 "\n========== 读取文件 ==========="。

印 "读取文件："连文件名。
定 读取内容 = 读文件 文件名。
印 "文件内容："。
印 读取内容。

注 ========== 追加内容 ==========
印 "\n========== 追加内容 ==========="。

定 追加内容 = "\n这是追加的内容"。
印 "追加内容到文件..."。

注 读取原内容
定 原内容 = 读文件 文件名。
定 新内容 = 原内容连追加内容。
写文件 文件名 新内容。

印 "追加后的内容："。
印 读文件 文件名。

注 ========== 文件存在检查 ==========
印 "\n========== 文件存在检查 ==========="。

定 文件存在 = 存在 文件名。
印 "文件存在："连文件存在。

定 不存在的文件 = "nonexistent.txt"。
定 不存在 = 存在 不存在的文件。
印 "不存在的文件："连不存在。

注 ========== 目录操作 ==========
印 "\n========== 目录操作 ==========="。

印 "列出当前目录文件："。
定 文件列表 = 列目录 "."。
遍历 文件 于 文件列表：
  印 文件。
。

注 ========== 实用示例 ==========
印 "\n========== 实用示例 ==========="。

注 示例1：日志记录器
印 "\n示例1：日志记录器"。

定 日志文件 = "app.log"。
定 当前时间 = 当前时间。

定 写日志 = 函 消息
  定 日志条目 = 当前时间连" - "连消息连"\n"。
  定 原日志 = 读文件 日志文件。
  定 新日志 = 原日志连日志条目。
  写文件 日志文件 新日志。
。

写日志 "应用程序启动"。
写日志 "用户登录"。
写日志 "执行操作"。

印 "日志内容："。
印 读文件 日志文件。

注 示例2：配置文件读写
印 "\n示例2：配置文件读写"。

定 配置文件 = "config.txt"。
定 配置内容 = "language=yan\nversion=1.0\ndebug=true"。

写文件 配置文件 配置内容。
印 "配置文件已创建"。

印 "读取配置："。
印 读文件 配置文件。

注 示例3：数据备份
印 "\n示例3：数据备份"。

定 源文件 = "test_file.txt"。
定 备份文件 = "test_file_backup.txt"。

印 "备份文件："连源文件连" → "连备份文件。
定 源内容 = 读文件 源文件。
写文件 备份文件 源内容。
印 "备份完成！"。

注 示例4：批量文件处理
印 "\n示例4：批量文件处理"。

印 "统计当前目录文件数量："。
定 所有文件 = 列目录 "."。
定 文件数量 = 长 所有文件。
印 "文件总数："连文件数量。

注 示例5：文件内容分析
印 "\n示例5：文件内容分析"。

定 分析文件 = 函 文件路径
  定 内容 = 读文件 文件路径。
  定 行数 = 长 分割 内容 "\n"。
  定 字符数 = 长 内容。
  
  印 "文件："连文件路径。
  印 "行数："连行数。
  印 "字符数："连字符数。
。

分析文件 文件名。

注 ========== 清理测试文件 ==========
印 "\n========== 清理测试文件 ==========="。

印 "注意：测试文件已创建在当前目录"。
印 "文件列表："。
印 "  - test_file.txt"。
印 "  - test_file_backup.txt"。
印 "  - app.log"。
印 "  - config.txt"。
