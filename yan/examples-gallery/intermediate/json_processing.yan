注 JSON处理 - JSON解析和生成
注 难度：⭐⭐⭐
注 学习目标：掌握JSON数据的解析、生成和操作

印 "=== JSON处理示例 ==="。

导入 JSON。

注 ========== JSON编码 ==========
印 "\n========== JSON编码 ==========="。

注 字典转JSON
印 "\n--- 字典转JSON ---"。

定 用户信息 = 典 
  "name" "张三"
  "age" 25
  "email" "zhangsan@example.com"
  "active" 真
。

印 "字典数据："连用户信息。

定 JSON字符串 = JSON.编码 用户信息。
印 "JSON字符串："连JSON字符串。

注 格式化JSON
印 "\n--- 格式化JSON ---"。

定 格式化JSON = JSON.格式化 用户信息 2。
印 "格式化JSON："。
印 格式化JSON。

注 列表转JSON
印 "\n--- 列表转JSON ---"。

定 成绩列表 = 列 85 92 78 90 88。
印 "列表数据："连成绩列表。

定 列表JSON = JSON.编码 成绩列表。
印 "JSON字符串："连列表JSON。

注 复杂嵌套结构
印 "\n--- 复杂嵌套结构 ---"。

定 复杂数据 = 典
  "user" 典
    "name" "李四"
    "profile" 典
      "age" 30
      "city" "上海"
  "scores" 列 95 88 92
  "metadata" 典
    "created" "2026-05-18"
    "updated" "2026-05-18"
。

印 "复杂数据："。
印 JSON.格式化 复杂数据 2。

注 ========== JSON解码 ==========
印 "\n========== JSON解码 ==========="。

注 解码JSON字符串
印 "\n--- 解码JSON字符串 ---"。

定 JSON输入 = "{\"name\":\"王五\",\"age\":28,\"city\":\"广州\"}"。
印 "JSON输入："连JSON输入。

定 解码数据 = JSON.解码 JSON输入。
印 "解码结果："连解码数据。

注 访问解码后的数据
印 "\n--- 访问解码后的数据 ---"。

定 姓名 = 取 解码数据 "name"。
定 年龄 = 取 解码数据 "age"。
定 城市 = 取 解码数据 "city"。

印 "姓名："连姓名。
印 "年龄："连年龄。
印 "城市："连城市。

注 解码列表
印 "\n--- 解码列表 ---"。

定 列表JSON输入 = "[1, 2, 3, 4, 5]"。
定 解码列表 = JSON.解码 列表JSON输入。
印 "解码列表："连解码列表。

注 ========== JSON文件操作 ==========
印 "\n========== JSON文件操作 ==========="。

注 写入JSON文件
印 "\n--- 写入JSON文件 ---"。

定 配置数据 = 典
  "app_name" "言语言应用"
  "version" "1.0.0"
  "settings" 典
    "debug" 假
    "log_level" "INFO"
    "max_connections" 100
。

定 配置文件 = "config.json"。
JSON.写文件 配置文件 配置数据。
印 "配置已写入："连配置文件。

注 读取JSON文件
印 "\n--- 读取JSON文件 ---"。

定 读取配置 = JSON.读文件 配置文件。
印 "读取的配置："。
印 JSON.格式化 读取配置 2。

注 ========== JSONPath提取 ==========
印 "\n========== JSONPath提取 ==========="。

印 "使用JSONPath提取嵌套数据："。

定 嵌套数据 = 典
  "store" 典
    "book" 列
      典 "title" "书名1" "price" 10
      典 "title" "书名2" "price" 20
    "bicycle" 典 "color" "red" "price" 100
。

印 "原始数据："。
印 JSON.格式化 嵌套数据 2。

注 提取特定路径
印 "\n提取所有书籍标题："。
定 书籍标题 = JSON.提取 嵌套数据 "$.store.book[*].title"。
印 书籍标题。

印 "\n提取所有价格："。
定 所有价格 = JSON.提取 嵌套数据 "$..price"。
印 所有价格。

注 ========== 实用示例 ==========
印 "\n========== 实用示例 ==========="。

注 示例1：API响应处理
印 "\n示例1：API响应处理"。

定 API响应 = "{\"status\":\"success\",\"data\":{\"users\":[{\"id\":1,\"name\":\"Alice\"},{\"id\":2,\"name\":\"Bob\"}]},\"message\":\"OK\"}"。

定 响应数据 = JSON.解码 API响应。
印 "API响应："。
印 JSON.格式化 响应数据 2。

定 状态 = 取 响应数据 "status"。
定 消息 = 取 响应数据 "message"。
印 "状态："连状态。
印 "消息："连消息。

注 示例2：数据转换
印 "\n示例2：数据转换"。

定 CSV格式 = "name,age,city\n张三,25,北京\n李四,30,上海\n王五,28,广州"。
印 "CSV数据："。
印 CSV格式。

注 转换为JSON
定 行列表 = 分割 CSV格式 "\n"。
印 "转换为JSON格式..."。

定 结果列表 = 列。
遍历 行 于 余 行列表：
  定 字段 = 分割 行 ","。
  定 记录 = 典
    "name" 取 字段 0
    "age" 取 字段 1
    "city" 取 字段 2
  。
  定 结果列表 = 入 记录 结果列表。
。

印 "JSON格式："。
印 JSON.格式化 结果列表 2。

注 示例3：配置合并
印 "\n示例3：配置合并"。

定 默认配置 = 典
  "debug" 假
  "log_level" "INFO"
  "timeout" 30
。

定 用户配置 = 典
  "debug" 真
  "log_level" "DEBUG"
。

印 "默认配置："连JSON.编码 默认配置。
印 "用户配置："连JSON.编码 用户配置。

注 合并配置（用户配置覆盖默认配置）
印 "合并后的配置："。
印 JSON.格式化 用户配置 2。

注 ========== 清理 ==========
印 "\n========== 清理 ==========="。

印 "JSON文件已创建：config.json"。
