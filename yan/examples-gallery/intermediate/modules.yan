注 模块系统 - 模块导入和导出
注 难度：⭐⭐⭐
注 学习目标：理解模块系统的工作原理和使用方法

印 "=== 模块系统示例 ==="。

注 ========== 标准库模块 ==========
印 "\n========== 标准库模块 ==========="。

注 导入数学模块
印 "\n--- 数学模块 ---"。

导入 数学。

印 "圆周率："连圆周率。
印 "sqrt(16) = "连开方 16。
印 "sin(π/2) = "连正弦 除 圆周率 2。
印 "cos(0) = "连余弦 0。
印 "floor(3.7) = "连取整 3.7。
印 "round(3.5) = "连四舍五入 3.5。

注 导入字符串模块
印 "\n--- 字符串模块 ---"。

导入 字符串。

定 文本 = "Hello World"。
印 "原文："连文本。
印 "长度："连长度 文本。
印 "大写："连大写 文本。
印 "小写："连小写 文本。
印 "分割："连分割 文本 " "。

注 导入时间模块
印 "\n--- 时间模块 ---"。

导入 时间。

印 "当前时间："连当前时间。
印 "当前日期："连日期。
印 "时间戳："连时间戳。

注 导入JSON模块
印 "\n--- JSON模块 ---"。

导入 JSON。

定 数据 = 典 "name" "张三" "age" 25 "city" "北京"。
印 "字典数据："连数据。

定 JSON字符串 = JSON.编码 数据。
印 "JSON编码："连JSON字符串。

定 解码数据 = JSON.解码 JSON字符串。
印 "JSON解码："连解码数据。

注 ========== 选择性导入 ==========
印 "\n========== 选择性导入 ==========="。

印 "从数学模块导入特定函数："。

从 数学 导入 开方 幂。

印 "sqrt(25) = "连开方 25。
印 "pow(2, 8) = "连幂 2 8。

注 ========== 自定义模块 ==========
印 "\n========== 自定义模块 ==========="。

注 创建自定义模块文件
印 "\n创建自定义模块：mymodule.yan"。

定 模块内容 = 
"# 自定义模块示例
导出 
  模块名 \"我的模块\"
  版本 \"1.0\"
  问候 函 名字 印 \"你好，\"连名字连\"！\"
  计算 函 a b 加 a b
。"。

写文件 "mymodule.yan" 模块内容。
印 "模块文件已创建"。

注 导入自定义模块
印 "\n导入自定义模块："。

导入 我的模块。

印 "模块名："连模块名。
印 "模块版本："连版本。
问候 "世界"。
印 "计算结果："连计算 10 20。

注 ========== 模块组合 ==========
印 "\n========== 模块组合 ==========="。

印 "组合多个模块的功能："。

导入 数学。
导入 字符串。
导入 时间。

定 生成报告 = 函 标题 数据
  定 时间戳 = 当前时间。
  定 格式化标题 = 大写 标题。
  
  定 报告 = 
    "=== "连格式化标题连" ===\n"连
    "生成时间："连时间戳连"\n"连
    "数据："连数据连"\n"连
    "统计："连长 数据连"项"。
  
  报告。
。

定 测试数据 = 列 "项目1" "项目2" "项目3"。
定 报告 = 生成报告 "数据报告" 测试数据。
印 报告。

注 ========== 模块最佳实践 ==========
印 "\n========== 模块最佳实践 ==========="。

印 "模块化开发建议："。
印 "1. 每个模块职责单一"。
印 "2. 使用明确的导出列表"。
印 "3. 避免循环依赖"。
印 "4. 提供清晰的模块文档"。
印 "5. 使用版本号管理模块"。

注 ========== 清理 ==========
印 "\n========== 清理 ==========="。

印 "自定义模块文件 mymodule.yan 已创建"。
印 "可以删除或保留作为参考"。
