注 字符串处理 - 字符串操作和格式化
注 难度：⭐⭐⭐
注 学习目标：掌握字符串的各种操作方法

印 "=== 字符串处理示例 ==="。

注 字符串基础
印 "\n--- 字符串基础 ---"。

定 问候 = "你好"。
定 名字 = "世界"。
定 完整 = 问候连"，"连名字连"！"。
印 完整。

注 字符串长度
印 "\n--- 字符串长度 ---"。

定 文本 = "言语言是一种文言风格的编程语言"。
定 长度 = 长 文本。
印 "文本："连文本。
印 "长度："连长度。

注 字符串查找
印 "\n--- 字符串查找 ---"。

定 句子 = "言语言是一种编程语言，言语言支持函数式编程"。
印 "句子："连句子。

定 是否包含 = 含 句子 "编程"。
印 "包含'编程'："连是否包含。

定 首次出现 = 找 句子 "语言"。
印 "首次出现'语言'："连首次出现。

注 字符串分割和连接
印 "\n--- 字符串分割和连接 ---"。

定 水果串 = "苹果,香蕉,橙子,葡萄"。
印 "原始字符串："连水果串。

定 水果列表 = 分割 水果串 ","。
印 "分割后："连水果列表。

定 新字符串 = 连接 水果列表 " | "。
印 "重新连接："连新字符串。

注 字符串替换
印 "\n--- 字符串替换 ---"。

定 原文 = "我喜欢Python编程"。
印 "原文："连原文。

定 替换后 = 替换 原文 "Python" "言语言"。
印 "替换后："连替换后。

注 大小写转换
印 "\n--- 大小写转换 ---"。

定 英文 = "Hello World"。
印 "原文："连英文。

定 小写 = 小写 英文。
印 "小写："连小写。

定 大写 = 大写 英文。
印 "大写："连大写。

注 去除空格
印 "\n--- 去除空格 ---"。

定 带空格 = "  hello world  "。
印 "原文：'"连带空格连"'"。

定 去两端 = 去空 带空格。
印 "去两端空格：'"连去两端连"'"。

注 字符串切片
印 "\n--- 字符串切片 ---"。

定 文本2 = "言语言编程"。
印 "文本："连文本2。

定 前三个 = 取前 文本2 3。
印 "前三个字符："连前三个。

定 后两个 = 取后 文本2 2。
印 "后两个字符："连后两个。

注 字符串格式化
印 "\n--- 字符串格式化 ---"。

定 姓名 = "张三"。
定 年龄 = 25。
定 分数 = 95.5。

印 "姓名："连姓名连"，年龄："连年龄连"，分数："连分数。

注 实用示例：生成报告
印 "\n--- 实用示例：生成报告 ---"。

定 学生姓名 = "李四"。
定 科目 = "数学"。
定 成绩 = 92。

定 报告 = "学生报告\n"连
           "姓名："连学生姓名连"\n"连
           "科目："连科目连"\n"连
           "成绩："连成绩连"分\n"连
           "等级："连若 大 成绩 90 则 "优秀" 否则 "良好"

印 报告。

注 实用示例：邮箱验证
印 "\n--- 实用示例：邮箱验证 ---"。

定 邮箱 = "user@example.com"。
印 "邮箱："连邮箱。

定 是否有效 = 含 邮箱 "@"。
印 "格式有效："连是否有效。

定 用户名 = 取前 邮箱 找 邮箱 "@"。
印 "用户名："连用户名。

注 实用示例：密码强度检查
印 "\n--- 实用示例：密码强度检查 ---"。

定 密码 = "Abc123!@#"。
印 "密码："连密码。

定 长度检查 = 大 长 密码 8。
印 "长度>8："连长度检查。

定 包含数字 = 含 密码 "1"。
印 "包含数字："连包含数字。

定 包含大写 = 含 密码 "A"。
印 "包含大写："连包含大写。

定 强度 = 与 长度检查 与 包含数字 包含大写。
印 "强度合格："连强度。
