定距离=函a b：
    定差=减a b。
    若差小0则负差否则差。
。

印"距离示例："。
印距离3 7。
印距离7 3。
印距离10 5。

定统计=函语文 数学 英语：
    定总分=$(语文 + 数学 + 英语)。
    定均值=$(总分 / 3)。
    列总分 均值。
。

印"成绩统计："。
印统计85 92 78。

定体积=函x y z：
    定底面积=$(x * y)。
    定结果=$(底面积 * z)。
    结果。
。

印"体积计算："。
印体积3 4 5。
