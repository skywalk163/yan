-- 简单冒泡排序
定义 冒泡排序 = 函数 arr
  定义 n = 长度 arr
  遍历 i 于 范围 n
    定义 已交换 = 假
    定义 边界 = 减 (减 n i) 1
    定义 j = 0
    当时 小于 j 边界
      定义 当前 = 取 arr j
      定义 下一个 = 取 arr (加 j 1)
      如果 大于 当前 下一个
        设 arr j 下一个
        设 arr (加 j 1) 当前
        定义 已交换 = 真
      定义 j = 加 j 1
    如果 非 已交换 那么 返回 arr
  返回 arr

-- 测试调用
定义 数据 = 列表 5 3 8 4 2。
输出 "排序前："。
输出 数据。
定义 结果 = 冒泡排序 数据。
输出 "排序后："。
输出 结果。