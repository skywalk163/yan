注 简单计算器

定 计算 = 函 表达式
  {{
import re
tokens = re.findall(r'(\d+\.?\d*|[+\-*/])', 表达式)
result = float(tokens[0])
for i in range(1, len(tokens), 2):
    op = tokens[i]
    num = float(tokens[i+1])
    if op == '+':
        result += num
    elif op == '-':
        result -= num
    elif op == '*':
        result *= num
    elif op == '/':
        result /= num
result
}}。

印 "简单计算器"。
印 "输入表达式（如：1+2*3）："。
定 输入 = 读。
定 结果 = 计算 输入。
印 "结果："连结果。