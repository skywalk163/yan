-- 交互式计算器
-- 输入数学表达式，自动计算结果
-- 输入 q 退出

输出 "=== 交互式计算器 ==="。
输出 "输入数学表达式求值，输入 q 退出"。
输出 ""。

输出 "示例：3 + 4 * 2"。
输出 "提示：支持 +, -, *, / 和括号 ()"。
输出 ""。

-- 循环读取输入并计算
-- 使用 读行 函数读取用户输入
定义 退出 = 假。

当时 非 退出：
  输出 "请输入表达式："。
  定义 输入 = 读行。
  
  -- 检查是否退出
  如果 等于 输入 "q"：
    输出 "再见！"。
    定义 退出 = 真。
  。
  如果 等于 输入 "Q"：
    输出 "再见！"。
    定义 退出 = 真。
  。
  如果 等于 输入 ""：
    -- 空输入，继续循环
    输出 ""。
  。
  如果 非 退出：
    -- 计算并输出结果
    定义 结果 = 求值 输入。
    输出 连接 "结果: " 结果。
    输出 ""。
  。
。
