-- JSON 操作示例

定用户 = {{ {'name': '张三', 'age': 25, 'hobbies': ['阅读', '编程']} }}
定json_str = 转JSON 用户。
印"用户信息 (JSON): " json_str。

定解析数据 = {{ __import__('json').loads('{"a": 1, "b": 2}') }}
印"解析后的数据: " 解析数据。
