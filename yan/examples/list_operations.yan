-- 列表操作示例

定 arr = 列 1 2 3 4 5。

印 "原始列表: " arr。
印 "长度: " 长 arr。
印 "第一个元素: " 首 arr。
印 "剩余元素: " 余 arr。

定 new_arr = 添 arr 6。
印 "添加元素后: " new_arr。

-- 使用高阶函数来映射
定 square = 函 x 乘 x 2。
定 doubled = 皆 square arr。
印 "加倍后的列表: " doubled。