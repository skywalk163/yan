-- 数学函数示例

印"π = " 圆周率。
印"e = " 自然常数。

定义 x = 4。
印"sqrt(x) = " 开方x。

定义 y = 16。
印"sqrt(y) = " 开方y。

定义 angle = 0。
印"sin(angle) = " 正弦 angle。
印"cos(angle) = " 余弦 angle。

印"随机数: " 随机。
印"随机整数 (0-10): " 随机整数 0 11。