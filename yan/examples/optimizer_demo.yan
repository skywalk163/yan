注 优化器演示示例
注 展示常量折叠、死代码消除等优化效果

注 ====================
注 示例1: 常量折叠
注 ====================
定义 圆周率值 = 圆周率。
定义 半径 = 2。
定义 面积 = 乘 圆周率值 乘 半径 半径。
输出 连接 "圆的面积: " 面积。

注 ====================
注 示例2: 死代码消除
注 ====================
定义 调试模式 = 假。
如果 调试模式 那么
  输出 "调试信息: 进入函数"
否则
  输出 "运行模式: 正常执行"

注 ====================
注 示例3: 布尔表达式简化
注 ====================
定义 条件1 = 真。
定义 条件2 = 小于 5 10。
如果 且 条件1 条件2 那么
  输出 "两个条件都满足"
否则
  输出 "条件不满足"

注 ====================
注 示例4: 数学表达式优化
注 ====================
定义 乘结果 = 乘 2 3。
定义 加结果 = 加 4 5。
定义 结果 = 加 乘结果 加结果。
输出 连接 "计算结果: " 结果。

注 ====================
注 示例5: 列表操作
注 ====================
定义 数据 = 列表 1 2 3 4 5。
输出 "原始列表:"。
输出 数据。

注 完成
输出 "优化器演示完成！"。
