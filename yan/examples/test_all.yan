-- 测试1：减法
印10减5。

-- 测试2：除法
印10除5。

-- 测试3：范围
印范围5。

-- 测试4：阶乘
定阶乘=函n若n小等于1则1否则n乘阶乘n减1。
印阶乘5。

-- 测试5：平方
定平方=函x乘x x。
印平方5。

-- 测试6：距离
定距离=函a b $(abs(a - b))。
印距离3 7。

-- 测试7：条件
印若5大3则"大"否则"小"。

-- 测试8：条件2
印若$(10 > 5)则真否则假。
