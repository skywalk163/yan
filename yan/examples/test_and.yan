-- 测试 且 表达式
输出 "测试开始"。

定义 有效 = 真。
定义 已用 = 列。
定义 你 = "A"。

输出 连接 "有效=" 有效。
输出 连接 "已用=" 已用。
输出 连接 "你=" 你。

定义 结果 = 包含 已用 你。
输出 连接 "包含 已用 你=" 结果。

定义 结果2 = 且 有效 包含 已用 你。
输出 连接 "且 有效 包含 已用 你=" 结果2。

输出 "测试结束"。