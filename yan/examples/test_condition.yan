-- 测试条件判断
输出 "=== 测试条件判断 ==="。

定义 值 = 5。

输出 "测试多个独立若语句："。
如果 等于 值 5：
  输出 "值等于5"。
。
如果 小于 值 10：
  输出 "值小于10"。
。
如果 大于 值 0：
  输出 "值大于0"。
。

输出 ""。
输出 "测试若否则语句："。
如果 等于 值 5：
  输出 "值等于5"。
否则：
  输出 "值不等于5"。
。

输出 ""。
输出 "=== 测试完成 ==="。