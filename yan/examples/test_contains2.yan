-- 测试列表包含（简化版）
输出 "测试开始"。

定义 数据 = 列表 "A" "B" "C"。

输出 连接 "列表=" 列表。
输出 连接 "类型=" 类型 列表。

定义 结果 = 包含 列表 "A"。
输出 连接 "包含 列表 \"A\" = " 结果。

-- 测试列表变量
定义 已用 = 列。
输出 连接 "已用=" 已用。
输出 连接 "已用类型=" 类型 已用。

定义 x = "A"。
输出 连接 "x=" x。
输出 连接 "x类型=" 类型 x。

定义 r = 包含 已用 x。
输出 连接 "包含 已用 x = " r。

输出 "测试结束"。