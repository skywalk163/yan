-- 测试函数定义和调用
输出 "开始测试"。

定义 求和 = 函数 列表
  输出 "进入求和函数"。
  定义 结果 = 0。
  遍历 数 于 列表
    输出 "当前数：" 数。
    定义 结果 = 加 结果 数。
  输出 "结果：" 结果。
  返回 结果。

输出 "定义列表"。
定义 数据 = 列表 1 2 3。
输出 "列表：" 列表。

输出 "调用求和"。
定义 总 = 求和 列表。
输出 "总和：" 总。