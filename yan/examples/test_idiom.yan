-- 测试成语接龙（预设输入）
-- 言语言实现

输出 "=== 成语接龙测试 ==="。

定义 库 = 列
  "一心一意" "意气风发" "发愤图强"
。

定义 已用 = 列。
定义 尾 = ""。

定义 你 = "一心一意"。
输出 连接 "你：" 你。

添加 已用 你。
定义 尾 = 截取 你 3 4。
输出 连接 "尾字：" 尾。

定义 选 = 列。
遍历 y 于 库：
  如果 非 包含 已用 y：
    定义 z = 截取 y 0 1。
    如果 等于 z 尾：
      添加 选 y。
    。
  。
。

输出 连接 "可选：" 选。

-- 使用两个独立的 if 语句替代 if-else
如果 空 选：
  输出 "AI接不上了！"。
。

如果 非 空 选：
  定义 选长度 = 长度 选。
  输出 连接 "可选数量：" 选长。
  定义 i = {{ __import__('random').randint(0, 选长度 - 1) }}。
  定义 r = 取 选 i。
  输出 连接 "AI：" r。
。

输出 "测试完成"。