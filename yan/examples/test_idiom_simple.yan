-- 简化成语接龙测试
输出 "=== 简化成语接龙测试 ==="。

-- 成语库
定义 库 = 列
  "一心一意" "意气风发" "发愤图强" "强词夺理" "理直气壮"。

-- 游戏状态
定义 已用 = 列。
定义 尾 = ""。

-- 第一轮
定义 你 = "一心一意"。
添加 已用 你。
定义 尾 = 截取 你 3 4。
输出 连接 "你：" 你 连接 "（尾字：" 尾 "）"。

-- AI 选择
定义 选 = 列。
遍历 y 于 库：
  定义 z = 截取 y 0 1。
  定义 已用过 = 包含 已用 y。
  定义 匹配 = 且 非 已用过 等于 z 尾。
  如果 匹配：
    添加 选 y。
  。
。

输出 连接 "可选数量：" 长度 选。

如果 非 空 选：
  定义 选长度 = 长度 选。
  定义 i = {{ __import__('random').randint(0, 选长度 - 1) }}。
  定义 r = 取 选 i。
  添加 已用 r。
  定义 尾 = 截取 r 3 4。
  输出 连接 "AI：" r 连接 "（尾字：" 尾 "）"。
。

输出 "结束"。
