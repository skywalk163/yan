-- 测试函数和循环的缩进语法
定义 求和 = 函数 列表
  定义 结果 = 0
  遍历 数 于 列表
    定义 结果 = 加 结果 数
  返回 结果

定义 数据 = 列表 1 2 3 4 5。
输出 "列表：" 列表。
输出 "总和：" (求和 列表)。

-- 测试嵌套循环
定义 九九乘法表 = 函
  遍历 i 于 范围 1 10
    定义 行 = 空 串
    遍历 j 于 范围 1 (加 i 1)
      定义 乘积 = 乘 i j
      定义 项 = 连接 i "x" j "=" 乘积 " "
      定义 行 = 连接 行 项
    输出 行

九九乘法表。