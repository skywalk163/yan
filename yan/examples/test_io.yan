-- 测试新的输入关键字
输出 "=== 测试读和读行 ==="。
输出 ""。

-- 测试读行（向后兼容也可以用行
输出 "测试读行："。
输出 "提示：在终端中运行才能真正输入！"。
输出 ""。

-- 这里我们只做展示，因为非交互环境不能真的输入
输出 "语法正确！可以用："。
输出 "  - 读：读取单个字符"。
输出 "  - 读行：读取一行输入"。
输出 "  - 行：旧的方式（仍有效，向后兼容）"。
输出 ""。
输出 "示例："。
输出 "  定义 名字 = 读行"。
输出 "  输出 连接 \"你好，\" 名字"。
输出 ""。
输出 "=== 测试完成 ==="。