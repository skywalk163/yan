-- AST节点定义模块（纯言语言实现）

-- AST节点类型常量
定AST程序=1。
定AST数字=2。
定AST字符串=3。
定AST布尔=4。
定AST空=5。
定AST词=6。
定AST定义=7。
定AST函数=8。
定AST调用=9。
定AST条件=10。
定AST循环=11。
定AST代码=12。

-- 创建AST节点
定创建程序=函语句列表：
  定节点=列AST程序语句列表。
  返回节点。
。

定创建数字=函值：
  定节点=列AST数字值。
  返回节点。
．

定创建字符串=函值：
  定节点=列AST字符串值。
  返回节点。
。

定创建布尔=函值：
  定节点=列AST布尔值。
  返回节点。
．

定创建空=函：
  定节点=列AST空。
  返回节点。
．

定创建词=函名称：
  定节点=列AST词名称。
  返回节点。
．

定创建定义=函名称值：
  定节点=列AST定义名称值。
  返回节点。
．

定创建函数=函参数体：
  定节点=列AST函数参数体。
  返回节点。
．

定创建调用=函动词参数：
  定节点=列AST调用动词参数。
  返回节点。
．

定创建条件=函条件则分支否则分支：
  定节点=列AST条件条件则分支否则分支。
  返回节点。
．

定创建循环=函条件体：
  定节点=列AST循环条件体。
  返回节点。
．

定创建代码=函代码：
  定节点=列AST代码代码。
  返回节点。
．

-- 获取节点类型
定取节点类型=函节点：
  定类型=入节点0。
  返回类型。
．

-- 获取节点属性
定取程序语句=函节点：
  定语句=入节点1。
  返回语句。
．

定取数字值=函节点：
  定值=入节点1。
  返回值。
．

定取字符串值=函节点：
  定值=入节点1。
  返回值。
．

定取布尔值=函节点：
  定值=入节点1。
  返回值。
．

定取词名称=函节点：
  定名称=入节点1。
  返回名称。
．

定取定义名称=函节点：
  定名称=入节点1。
  返回名称。
．

定取定义值=函节点：
  定值=入节点2。
  返回值。
．

定取函数参数=函节点：
  定参数=入节点1。
  返回参数。
．

定取函数体=函节点：
  定体=入节点2。
  返回体。
．

定取调用动词=函节点：
  定动词=入节点1。
  返回动词。
．

定取调用参数=函节点：
  定参数=入节点2。
  返回参数。
．

定取条件条件=函节点：
  定条件=入节点1。
  返回条件。
．

定取条件则分支=函节点：
  定则分支=入节点2。
  返回则分支。
．

定取条件否则分支=函节点：
  定否则分支=入节点3。
  返回否则分支。
．

定取循环条件=函节点：
  定条件=入节点1。
  返回条件。
．

定取循环体=函节点：
  定体=入节点2。
  返回体。
．

定取代码代码=函节点：
  定代码=入节点1。
  返回代码。
．
