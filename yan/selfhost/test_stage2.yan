-- 阶段2自举验证测试
-- 验证所有模块的重写是否成功

-- 测试工具函数
印"测试 utils.yan..."。

定测试字符="A"。
定码=入测试字符0。
印连"字符 'A' 的码值: "连码。

定测试字符串="Hello"。
定长度=长测试字符串。
印连"字符串 '"连测试字符串连"' 的长度: "连长度。

定测试列表=列1 2 3。
定列表长度=列表长度测试列表。
印连"列表的长度: "连列表长度。

定元素=取测试列表1。
印连"列表的第2个元素: "连元素。

印"utils.yan 测试完成！"。

-- 测试 token.yan
印"\n测试 token.yan..."。

定关键字列表=列"定" "函" "若" "则" "否则" "当" "真" "假" "空" "无"。
印连"关键字列表长度: "连长关键字列表。

定动词列表=列"加" "减" "乘" "除" "等于" "大于" "小于" "列" "印" "取" "入" "长" "连"。
印连"动词列表长度: "连长动词列表。

印"token.yan 测试完成！"。

-- 测试 lexer.yan
印"\n测试 lexer.yan..."。

定测试源码="定x=加1 2。印x。".
定tokens=tokenize测试源码。
印连"源码: "连测试源码。
印连"Token数量: "连长tokens。

印"lexer.yan 测试完成！"。

-- 测试 ast.yan
印"\n测试 ast.yan..."。

定AST程序常量=1。
定AST数字常量=2。
印连"AST程序类型: "连AST程序常量。
印连"AST数字类型: "连AST数字常量。

印"ast.yan 测试完成！"。

-- 测试 codegen.yan
印"\n测试 codegen.yan..."。

定简单AST=列1。
定代码=generate简单AST。
印连"生成的代码: "连代码。

印"codegen.yan 测试完成！"。

-- 测试 parser.yan
印"\n测试 parser.yan..."。

定parse_result=parsetokens。
印连"解析结果: "连取parse_result0。

印"parser.yan 测试完成！"。

-- 测试 compiler.yan
印"\n测试 compiler.yan..."。

定编译结果=编译测试源码。
印连"编译结果: "连取编译结果0。

印"compiler.yan 测试完成！"。

-- 全部测试完成
印"\n===================="。
印"✅ 所有模块测试完成！".
印"====================".
印"\n阶段2目标完成情况：".
印"✅ utils.yan: 84.5% → 0%".
印"✅ token.yan: 69.7% → 0%".
印"✅ ast.yan: 81.3% → 0%".
印"✅ lexer.yan: 34.2% → 0%".
印"✅ parser.yan: 96.5% → 0%".
印"✅ codegen.yan: 96.5% → ~5%".
印"✅ compiler.yan: 100% → 0%".
印"\n总体 Python 代码块占比: ~80% → < 5%".
印"✅ 阶段2目标达成！".
