-- 工具函数模块（纯言语言实现）

-- 中文字符判断边界值
定中文开始=2047。
定中文结束=65535。

-- ASCII 码边界值
定数字开始=48。
定数字结束=57。
定大写开始=65。
定大写结束=90。
定小写开始=97。
定小写结束=122。
定空白码=32。

-- 字符判断函数
定是中文字符=函字符：
  定码=入字符0
  返回且码大于中文开始码小于等于中文结束

定是数字=函字符：
  定码=入字符0
  返回且码大于等于数字开始码小于等于数字结束

定是字母=函字符：
  定码=入字符0
  返回或且码大于等于大写开始码小于等于大写结束且码大于等于小写开始码小于等于小写结束

定是空白=函字符：
  定码=入字符0
  返回码等于空白码

-- 标点符号集合（使用 Python 代码块，但保持简洁）
定标点=集"，。。：；！？""''（）【】{}（）@#$%^&*+-=~`|\\"。

定是标点=函字符：
  定码=入字符0
  或码等于34或码等于39或码等于40或码等于41或码等于44或码等于46或码等于58或码等于59或码等于63或码等于91或码等于93或码等于123或码等于125或码等于8203

-- 字符串操作
定截取=函字符串起始结束：
  定结果=""
  定i=起始
  当i小于结束：
    结果=连结果入字符串i
    i=加i1
  返回结果

定入=函字符串索引：
  定长度=长字符串
  当或索引小于0或索引大于等于长度：
    返回""
  定结果=""
  定i=0
  当i小于索引：
    i=加i1
  定c=入字符串i
  返回c

定长=函字符串：
  定计数=0
  当真：
    当计数小于100000：
      定c=入字符串计数
      当c等于""：
        返回计数
      计数=加计数1
    返回计数

-- 列表操作
定列表添=函列表项：
  连列表列项

定列表连=函列表1列表2：
  连列表1列表2

定取=函列表索引：
  定长度=列表长度列表
  当或索引小于0或索引大于等于长度：
    返回空
  定计数=0
  当计数小于索引：
    计数=加计数1
  定当前=列表0
  定i=0
  当i小于计数：
    当前=列表i
    i=加i1
  当真：
    当i等于索引：
      返回列表i
    i=加i1

定列表长度=函列表：
  定计数=0
  当真：
    当计数小于100000：
      定c=列表计数
      当c等于空：
        返回计数
      计数=加计数1
    返回计数

-- 元组操作
定入=函元组索引：
  定长度=长元组
  当或索引小于0或索引大于等于长度：
    返回空
  定计数=0
  当计数小于索引：
    计数=加计数1
  定当前=元组0
  定i=0
  当i小于计数：
    当前=元组i
    i=加i1
  当真：
    当i等于索引：
      返回元组i
    i=加i1

