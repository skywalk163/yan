-- 缓存模块（cache.yan）
-- 提供内存缓存和文件缓存功能

-- 导出声明
出 初始化缓存, 设置TTL, 设置文件缓存目录,
   缓存获取, 缓存设置, 缓存删除, 缓存清空, 缓存大小,
   内存缓存获取, 内存缓存设置, 设置最大缓存大小。

定 内存缓存 = 典。
定 文件缓存目录 = ".cache"。
定 默认TTL = 3600。
定 最大缓存大小 = 100。

定 设置最大缓存大小 = 函 大小：
  设 最大缓存大小 大小。
．

定 检查缓存大小限制 = 函：
  当 长 键列表 内存缓存 大于等于 最大缓存大小：
    定 键列表 = 键列表 内存缓存。
    当 长 键列表 大于 0：
      删 内存缓存 (取 键列表 0)。
    。
  。
．

定 初始化缓存 = 函：
  当 非 存在文件 文件缓存目录：
    执行命令 连 "mkdir -p " 文件缓存目录。
  。
.

定 设置TTL = 函 秒数：
  设 默认TTL 秒数。
.

定 设置文件缓存目录 = 函 目录：
  设 文件缓存目录 目录。
  当 非 存在文件 目录：
    执行命令 连 "mkdir -p " 目录。
  。
.

定 内存缓存获取 = 函 键：
  当 存在键 内存缓存 键：
    定 条目 = 值 内存缓存 键。
    当 当前时间戳 小于 值 条目 "过期时间"：
      返回 值 条目 "值"。
    。
    删 内存缓存 键。
  。
  返回 空。
.

定 内存缓存设置 = 函 键 值 过期时间：
  当 过期时间 等于 空：
    设 过期时间 加 当前时间戳 默认TTL。
  。
  检查缓存大小限制。
  设 内存缓存 键 典 "值" 值 "过期时间" 过期时间。
．.

定 内存缓存删除 = 函 键：
  删 内存缓存 键。
.

定 内存缓存清空 = 函：
  设 内存缓存 典。
.

定 文件缓存获取 = 函 键：
  定 文件名 = 连 文件缓存目录 "/" 键 ".cache"。
  当 存在文件 文件名：
    定 内容 = 读文件 文件名。
    定 条目 = JSON解析 内容。
    当 当前时间戳 小于 值 条目 "过期时间"：
      返回 值 条目 "值"。
    。
    删除文件 文件名。
  。
  返回 空。
.

定 文件缓存设置 = 函 键 值 过期时间：
  当 过期时间 等于 空：
    设 过期时间 加 当前时间戳 默认TTL。
  。
  定 条目 = 典 "值" 值 "过期时间" 过期时间。
  定 文件名 = 连 文件缓存目录 "/" 键 ".cache"。
  写文件 文件名 JSON生成 条目。
.

定 文件缓存删除 = 函 键：
  定 文件名 = 连 文件缓存目录 "/" 键 ".cache"。
  当 存在文件 文件名：
    删除文件 文件名。
  。
.

定 文件缓存清空 = 函：
  执行命令 连 "rm -rf " 文件缓存目录 "/*"。
.

定 缓存获取 = 函 键：
  定 值 = 内存缓存获取 键。
  当 值 不等于 空：
    返回 值。
  。
  返回 文件缓存获取 键。
.

定 缓存设置 = 函 键 值 TTL：
  内存缓存设置 键 值 空。
  当 TTL 不等于 空：
    文件缓存设置 键 值 加 当前时间戳 TTL。
  。
.

定 缓存删除 = 函 键：
  内存缓存删除 键。
  文件缓存删除 键。
.

定 缓存清空 = 函：
  内存缓存清空。
  文件缓存清空。
.

定 缓存大小 = 函：
  返回 长 键列表 内存缓存。
.
