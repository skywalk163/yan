-- 集合操作模块（collections.yan）
-- 提供高级集合操作函数

-- 导出声明
出 创建集合, 添集合, 含集合,
    交集, 并集, 差集,
    取键, 取值, 典长, 典空, 典合, 典滤, 典映,
    去重, 分组, 排序, 反转, 展平, 切片,
    查找, 查找所有, 存在, 所有, 范围。

-- 创建集合
定 创建集合 = 函 ...：
  定 结果 = 典。
  定 i = 0。
  当 i 小于 长参数：
    定 元素 = 参数i。
    定 结果 = 典 元素 真。
    i = 加 i 1。
  。
  返回 结果。
．

-- 添加元素到集合
定 添集合 = 函 集合 元素：
  定 集合 = 典 元素 真。
  返回 集合。
．

-- 判断元素是否在集合中
定 含集合 = 函 集合 元素：
  定 val = 入 集合 元素。
  当 val 等于 空：返回 假。。
  返回 val。
．

-- 集合交集
定 交集 = 函 集合1 集合2：
  定 结果 = 典。
  定 键 = 取键 集合1。
  定 i = 0。
  当 i 小于 长键：
    定 k = 键i。
    当 含集合 集合2 k：定 结果 = 典 k 真。。
    i = 加 i 1。
  。
  返回 结果。
．

-- 集合并集
定 并集 = 函 集合1 集合2：
  定 结果 = 典。
  定 键1 = 取键 集合1。
  定 i = 0。
  当 i 小于 长键1：定 k = 键1i。定 结果 = 典 k 真。i = 加 i 1。。
  定 键2 = 取键 集合2。
  i = 0。
  当 i 小于 长键2：定 k = 键2i。定 结果 = 典 k 真。i = 加 i 1。。
  返回 结果。
．

-- 集合差集
定 差集 = 函 集合1 集合2：
  定 结果 = 典。
  定 键 = 取键 集合1。
  定 i = 0。
  当 i 小于 长键：定 k = 键i。当 不 含集合 集合2 k：定 结果 = 典 k 真。。i = 加 i 1。。
  返回 结果。
．

-- 获取字典的所有键
定 取键 = 函 典：
  当 典 等于 空：返回 列。。
  返回 典。
．

-- 获取字典的所有值
定 取值 = 函 典：
  当 典 等于 空：返回 列。。
  定 结果 = 列。
  定 键 = 取键 典。
  定 i = 0。
  当 i 小于 长键：定 k = 键i。定 v = 入 典 k。连 结果 列 v。i = 加 i 1。。
  返回 结果。
．

-- 获取字典的键值对数量
定 典长 = 函 典：
  当 典 等于 空：返回 0。。
  定 键 = 取键 典。
  长 键。
．

-- 判断字典是否为空
定 典空 = 函 典：
  当 典 等于 空：返回 真。。
  长 典。
．

-- 字典合并
定 典合 = 函 典1 典2：
  定 结果 = 典1。
  定 键 = 取键 典2。
  定 i = 0。
  当 i 小于 长键：定 k = 键i。定 v = 入 典2 k。定 结果 = 典 k v。i = 加 i 1。。
  返回 结果。
．

-- 字典过滤
定 典滤 = 函 典 条件：
  定 结果 = 典。
  定 键 = 取键 典。
  定 i = 0。
  当 i 小于 长键：
    定 k = 键i。
    定 v = 入 典 k。
    当 条件 v：定 结果 = 典 k v。。
    i = 加 i 1。
  。
  返回 结果。
．

-- 字典映射
定 典映 = 函 典 变换：
  定 结果 = 典。
  定 键 = 取键 典。
  定 i = 0。
  当 i 小于 长键：
    定 k = 键i。
    定 v = 入 典 k。
    定 新v = 变换 v。
    定 结果 = 典 k 新v。
    i = 加 i 1。
  。
  返回 结果。
．

-- 列表去重
定 去重 = 函 列表：
  当 列表 等于 空：返回 列。。
  定 结果 = 典。
  定 i = 0。
  当 i 小于 长列表：定 元素 = 入列表i。定 结果 = 典 元素 真。i = 加 i 1。。
  定 键 = 取键 结果。
  返回 键。
．

-- 列表分组
定 分组 = 函 列表 键函数：
  定 结果 = 典。
  定 i = 0。
  当 i 小于 长列表：
    定 元素 = 入列表i。
    定 k = 键函数 元素。
    当 结果 等于 空：定 结果 = 典 k 列元素。。
    定 组 = 入 结果 k。
    连 组 列 元素。
    i = 加 i 1。
  。
  返回 结果。
．

-- 列表排序
定 排序 = 函 列表 比较函数：
  当 列表 等于 空：返回 列。。
  定 结果 = 列。
  定 i = 1。
  当 i 小于 长结果：
    定 j = 0。
    当 j 小于 减 长结果 1：
      定 a = 入 结果 j。
      定 b = 入 结果 加 j 1。
      当 比较函数 a b：定 结果 = 典 j b。定 结果 = 典 加j1 a。。
      j = 加 j 1。
    。
    i = 加 i 1。
  。
  返回 结果。
．

-- 列表反转
定 反转 = 函 列表：
  当 列表 等于 空：返回 列。。
  定 结果 = 列。
  定 i = 减 长列表 1。
  当 i 大于等于 0：
    定 元素 = 入列表i。
    连 结果 列 元素。
    i = 减 i 1。
  。
  返回 结果。
．

-- 展平列表
定 展平 = 函 列表：
  定 结果 = 列。
  定 i = 0。
  当 i 小于 长列表：
    定 元素 = 入列表i。
    当 元素 等于 空：i = 加 i 1。。
    当 且 长元素 大于 0：
      定 j = 0。
      当 j 小于 长元素：
        定 子 = 元素j。
        连 结果 列 子。
        j = 加 j 1。
      。
    。
    i = 加 i 1。
  。
  返回 结果。
．

-- 列表切片
定 切片 = 函 列表 起始 结束：
  当 列表 等于 空：返回 列。。
  定 结果 = 列。
  定 i = 起始。
  当 i 小于 结束：
    当 i 小于 长列表：定 元素 = 入列表i。连 结果 列 元素。。
    i = 加 i 1。
  。
  返回 结果。
．

-- 列表查找第一个匹配元素
定 查找 = 函 列表 条件：
  定 i = 0。
  当 i 小于 长列表：
    定 元素 = 入列表i。
    当 条件 元素：返回 元素。。
    i = 加 i 1。
  。
  返回 空。
．

-- 列表查找所有匹配元素
定 查找所有 = 函 列表 条件：
  定 结果 = 列。
  定 i = 0。
  当 i 小于 长列表：
    定 元素 = 入列表i。
    当 条件 元素：连 结果 列 元素。。
    i = 加 i 1。
  。
  返回 结果。
．

-- 列表是否存在满足条件的元素
定 存在 = 函 列表 条件：
  定 i = 0。
  当 i 小于 长列表：
    定 元素 = 入列表i。
    当 条件 元素：返回 真。。
    i = 加 i 1。
  。
  返回 假。
．

-- 列表是否所有元素都满足条件
定 所有 = 函 列表 条件：
  定 i = 0。
  当 i 小于 长列表：
    定 元素 = 入列表i。
    当 条件 元素：i = 加 i 1。。
    返回 假。
  。
  返回 真。
．

-- 创建范围列表
定 范围 = 函 开始 结束 步长：
  当 结束 等于 空：定 结束 = 开始。定 开始 = 0。定 步长 = 1。。
  当 步长 等于 空：定 步长 = 1。。
  定 结果 = 列。
  定 i = 开始。
  当 步长 大于 0：
    当 i 小于 结束：连 结果 列 i。i = 加 i 步长。
  。
  当 步长 小于 0：
    当 i 大于 结束：连 结果 列 i。i = 加 i 步长。
  。
  返回 结果。
．