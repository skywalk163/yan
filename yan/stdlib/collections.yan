-- 集合操作模块（collections.yan）

-- 注：本模块提供高级集合操作函数

-- 创建集合
定创建集合=函...
  定结果=典。
  定i=0。
  当i小于长参数：
    定元素=参数i。
    定结果=典元素真。
    i=加i1。
  。
  返回结果。
．

-- 添加元素到集合
定添集合=函集合元素：
  定集合=典元素真。
  返回集合。
．

-- 判断元素是否在集合中
定含集合=函集合元素：
  定val=入集合元素。
  当val等于空：返回假。。返回val。
．

-- 集合交集
定交集=函集合1集合2：
  定结果=典。
  定键=取键集合1。
  定i=0。
  当i小于长键：
    定k=键i。
    当含集合集合2k：定结果=典k真。。i=加i1。。返回结果。
  。
．

-- 集合并集
定并集=函集合1集合2：
  定结果=典。
  定键1=取键集合1。
  定i=0。当i小于长键1：定k=键1i。定结果=典k真。i=加i1。。定键2=取键集合2。i=0。当i小于长键2：定k=键2i。定结果=典k真。i=加i1。。返回结果。
．

-- 集合差集
定差集=函集合1集合2：
  定结果=典。
  定键=取键集合1。
  定i=0。当i小于长键：定k=键i。当不含集合集合2k：定结果=典k真。。i=加i1。。返回结果。
．

-- 获取字典的所有键
定取键=函典：当典等于空：返回列。。返回典。
．

-- 获取字典的所有值
定取值=函典：
  当典等于空：返回列。。定结果=列。定键=取键典。定i=0。当i小于长键：定k=键i。定v=入典k。连结果列v。i=加i1。。返回结果。
．

-- 获取字典的键值对数量
定典长=函典：
  当典等于空：返回0。。定键=取键典。长键。
．

-- 判断字典是否为空
定典空=函典：当典等于空：返回真。。长典。
．

-- 字典合并
定典合=函典1典2：
  定结果=典1。
  定键=取键典2。
  定i=0。当i小于长键：定k=键i。定v=入典2k。定结果=典kv。i=加i1。。返回结果。
．

-- 字典过滤
定典滤=函典条件：
  定结果=典。
  定键=取键典。
  定i=0。当i小于长键：
    定k=键i。定v=入典k。当条件v：定结果=典kv。。i=加i1。
  。返回结果。
．

-- 字典映射
定典映=函典变换：
  定结果=典。
  定键=取键典。
  定i=0。当i小于长键：
    定k=键i。定v=入典k。定新v=变换v。定结果=典k新v。i=加i1。
  。返回结果。
．

-- 列表去重
定去重=函列表：
  当列表等于空：返回列。。定结果=典。定i=0。当i小于长列表：定元素=入列表i。定结果=典元素真。i=加i1。。定键=取键结果。返回键。
．

-- 列表分组
定分组=函列表键函数：
  定结果=典。
  定i=0。当i小于长列表：
    定元素=入列表i。定k=键函数元素。当结果等于空：定结果=典k列元素。。定组=入结果k。连组列元素。i=加i1。
  。返回结果。
．

-- 列表排序
定排序=函列表比较函数：
  当列表等于空：返回列。。定结果=列1。当i小于长结果：
    定j=0。当j小于长结果：
      定a=入结果j。定b=入结果加j1。当比较函数a b：定结果=典aj。定结果=典ajb。j=加j1。
    。i=加i1。
  。返回结果。
．

-- 列表反转
定反转=函列表：
  当列表等于空：返回列。。定结果=列。定i=减长列表1。当i大于等于0：
    定元素=入列表i。连结果列元素。i=减i1。
  。返回结果。
．

-- 展平列表
定展平=函列表：
  定结果=列。当i小于长列表：
    定元素=入列表i。当元素等于空：i=加i1。。当且长元素大于0取元素0大于0：定j=0。当j小于长元素：定子=元素j。连结果列子。j=加j1。。i=加i1。
  。返回结果。
．

-- 列表切片
定切片=函列表起始结束：
  当列表等于空：返回列。。定结果=列。定i=起始。当i小于结束：
    当i小于长列表：定元素=入列表i。连结果列元素。。i=加i1。
  。返回结果。
．

-- 列表查找第一个匹配元素
定查找=函列表条件：
  定i=0。当i小于长列表：
    定元素=入列表i。当条件元素：返回元素。。i=加i1。
  。返回空。
．

-- 列表查找所有匹配元素
定查找所有=函列表条件：
  定结果=列。定i=0。当i小于长列表：
    定元素=入列表i。当条件元素：连结果列元素。。i=加i1。
  。返回结果。
．

-- 列表是否存在满足条件的元素
定存在=函列表条件：
  定i=0。当i小于长列表：
    定元素=入列表i。当条件元素：返回真。。i=加i1。
  。返回假。
．

-- 列表是否所有元素都满足条件
定所有=函列表条件：
  定i=0。当i小于长列表：
    定元素=入列表i。当条件元素：i=加i1。。返回假。
  。返回真。
．

-- 创建范围列表
定范围=函开始结束步长：
  当结束等于空：定结束=开始。定开始=0。定步长=1。。当步长等于空：定步长=1。
  定结果=列。定i=开始。当步长大于0：
    当i小于结束：连结果列i。i=加i步长。
  。当步长小于0：
    当i大于结束：连结果列i。i=加i步长。
  。返回结果。
．
