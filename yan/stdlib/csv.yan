-- CSV处理模块（csv.yan）
-- 提供CSV文件读写功能

-- 导出声明
出 读CSV, 写CSV, 读CSV行, 写CSV行,
    CSV转字典列表, 字典列表转CSV,
    读CSV无标题, 写CSV无标题。

-- 读取CSV文件（带标题）
定 读CSV = 函 文件路径 分隔符：
  返回 CSV读取 文件路径。
．

-- 写入CSV文件（带标题）
定 写CSV = 函 文件路径 数据 分隔符：
  返回 CSV写入 文件路径 数据。
．

-- 逐行读取CSV（带标题）
定 读CSV行 = 函 文件路径 分隔符：
  返回 CSV读取 文件路径。
．

-- 逐行写入CSV
定 写CSV行 = 函 文件路径 标题 行生成器 分隔符：
  写CSV 文件路径 行生成器 空。
．

-- CSV字符串转字典列表
定 CSV转字典列表 = 函 CSV字符串 分隔符：
  返回 CSV解析 CSV字符串。
．

-- 字典列表转CSV字符串
定 字典列表转CSV = 函 数据 分隔符：
  返回 CSV序列化 数据。
．

-- 读取CSV文件（无标题）
定 读CSV无标题 = 函 文件路径 分隔符：
  返回 CSV读取纯文本 文件路径。
．

-- 写入CSV文件（无标题）
定 写CSV无标题 = 函 文件路径 数据 分隔符：
  返回 CSV写入纯文本 文件路径 数据。
．