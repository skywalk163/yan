-- IO模块（io.yan）
-- 提供文件和输入输出功能

-- 导出声明
出 读文件, 读行, 读前行, 读后行,
    写文件, 追加文件,
    存在文件, 是文件, 是目录,
    文件大小, 文件修改时间,
    复制文件, 移动文件, 删除文件,
    建目录, 删目录, 列目录,
    当前目录, 改目录,
    绝对路径, 取文件名, 取目录名, 取扩展名,
    连路径, 标准化路径,
    读输入, 印输入, 印错误, 刷新输出,
    读JSON, 写JSON, 解JSON, 生JSON,
    读CSV, 写CSV。

-- 读取文件内容
定 读文件 = 函 文件名：
  返回 读文件 文件名。
．

-- 读取文件所有行
定 读行 = 函 文件名：
  定 内容 = 读文件 文件名。
  返回 分割 内容 "\\n"。
．

-- 读取前n行
定 读前行 = 函 文件名 n：
  定 所有行 = 读行 文件名。
  定 结果 = 列。
  定 i = 0。
  当 且 i 小于 n i 小于 长 所有行：
    添 结果 取 所有行 i。
    定 i = 加 i 1。
  。
  返回 结果。
．

-- 读取后n行
定 读后行 = 函 文件名 n：
  定 所有行 = 读行 文件名。
  定 开始 = 减 长 所有行 n。
  当 开始 小于 0：
    定 开始 = 0。
  。
  定 结果 = 列。
  定 i = 开始。
  当 i 小于 长 所有行：
    添 结果 取 所有行 i。
    定 i = 加 i 1。
  。
  返回 结果。
．

-- 写入文件
定 写文件 = 函 文件名 内容：
  写文件 文件名 内容。
．

-- 追加到文件
定 追加文件 = 函 文件名 内容：
  追加文件 文件名 内容。
．

-- 检查文件是否存在
定 存在文件 = 函 文件名：
  返回 存在 文件名。
．

-- 检查是否为文件
定 是文件 = 函 文件名：
  返回 是文件 文件名。
．

-- 检查是否为目录
定 是目录 = 函 路径：
  返回 是目录 路径。
．

-- 获取文件大小
定 文件大小 = 函 文件名：
  返回 0。
．

-- 获取文件修改时间
定 文件修改时间 = 函 文件名：
  返回 当前时间戳。
．

-- 复制文件
定 复制文件 = 函 源 目标：
  定 内容 = 读文件 源。
  写文件 目标 内容。
．

-- 移动文件
定 移动文件 = 函 源 目标：
  复制文件 源 目标。
  删除文件 源。
．

-- 删除文件
定 删除文件 = 函 文件名：
  删文件 文件名。
．

-- 创建目录
定 建目录 = 函 目录名：
  建目录 目录名。
．

-- 删除目录
定 删目录 = 函 目录名：
  删目录 目录名。
．

-- 列出目录内容
定 列目录 = 函 目录名：
  返回 列目录 目录名。
．

-- 获取当前目录
定 当前目录 = 函：
  返回 当前目录。
．

-- 改变当前目录
定 改目录 = 函 目录名：
  无。
．

-- 获取绝对路径
定 绝对路径 = 函 路径：
  返回 路径。
．

-- 获取文件名
定 取文件名 = 函 路径：
  返回 文件名 路径。
．

-- 获取目录名
定 取目录名 = 函 路径：
  返回 目录名 路径。
．

-- 获取扩展名
定 取扩展名 = 函 文件名：
  返回 扩展名 文件名。
．

-- 连接路径
定 连路径 = 函 路径1 路径2：
  返回 连 路径1 "/" 路径2。
．

-- 标准化路径
定 标准化路径 = 函 路径：
  返回 路径。
．

-- 读取用户输入
定 读输入 = 函：
  返回 读行。
．

-- 打印提示并读取输入
定 印输入 = 函 提示：
  印 提示。
  返回 读行。
．

-- 打印错误信息
定 印错误 = 函 消息：
  印 消息。
．

-- 刷新输出缓冲
定 刷新输出 = 函：
  无。
．

-- 读取JSON文件
定 读JSON = 函 文件名：
  定 内容 = 读文件 文件名。
  返回 JSON解析 内容。
．

-- 写入JSON文件
定 写JSON = 函 文件名 数据：
  定 JSON串 = JSON生成 数据。
  写文件 文件名 JSON串。
．

-- 解析JSON字符串
定 解JSON = 函 字符串：
  返回 JSON解析 字符串。
．

-- 生成JSON字符串
定 生JSON = 函 数据：
  返回 JSON生成 数据。
．

-- 读取CSV文件
定 读CSV = 函 文件名：
  返回 CSV读取 文件名。
．

-- 写入CSV文件
定 写CSV = 函 文件名 数据：
  CSV写入 文件名 数据。
．