-- 日志模块（logging.yan）
-- 提供日志记录功能

-- 导出声明
出 DEBUG, INFO, WARNING, ERROR, CRITICAL,
   设置日志级别, 设置日志文件, 设置日志格式,
   设置最大文件大小, 设置滚动时机, 设置保留备份数,
   debug, info, warning, error, critical

定 DEBUG = 1。
定 INFO = 2。
定 WARNING = 3。
定 ERROR = 4。
定 CRITICAL = 5。

定 日志级别 = INFO。
定 日志文件 = 空。
定 日志格式 = "%(时间)s [%(级别)s] %(消息)s"。

-- 滚动配置
定 最大文件大小 = 10485760。  -- 默认 10MB
定 滚动时机 = 空。  -- 空表示不使用时间滚动
定 保留备份数 = 5。  -- 默认保留 5 个备份
定 上次滚动时间戳 = 空。  -- 上次滚动的时间戳

定 级别名称 = 典 DEBUG "DEBUG" INFO "INFO" WARNING "WARNING" ERROR "ERROR" CRITICAL "CRITICAL"。

定 格式化日志 = 函 级别 消息：
  定 时间 = 格式化时间 "%Y-%m-%d %H:%M:%S"
  定 级别名 = 值 级别名称 级别
  定 格式化消息 = 日志格式
  设 格式化消息 = 替换 格式化消息 "%(时间)s" 时间
  设 格式化消息 = 替换 格式化消息 "%(级别)s" 级别名
  设 格式化消息 = 替换 格式化消息 "%(消息)s" 消息
  返回 格式化消息


定 设置日志级别 = 函 级别：
  设 日志级别 级别


定 设置日志文件 = 函 文件路径：
  设 日志文件 文件路径


定 设置日志格式 = 函 格式：
  设 日志格式 格式


定 设置最大文件大小 = 函 大小：
  设 最大文件大小 大小


定 设置滚动时机 = 函 时机：
  设 滚动时机 时机
  设 上次滚动时间戳 当前时间戳


定 设置保留备份数 = 函 数量：
  设 保留备份数 数量


定 获取文件字节数 = 函 文件路径：
  定 文件信息 = 执行命令 "powershell -Command \"(Get-Item '" 文件路径 "').Length\"".
  当 文件信息 不等于 空：
    定 输出 = 取子串 文件信息 1 ((长度 文件信息) 减 2)
    返 解析整数 输出

  返 0


定 获取备份文件列表 = 函 原文件路径：
  定 目录 = 取目录 原文件路径
  定 原文件名 = 取文件名 原文件路径
  定 匹配模式 = 原文件名 ".*\.bak$"
  定 所有文件 = 列出目录 目录
  定 备份文件 = 过滤 函 文件：匹配 匹配模式 文件
  返回 排序 备份文件


定 滚动时机到了 = 函：
  当 滚动时机 等于 空：
    返回 假

  当 上次滚动时间戳 等于 空：
    设 上次滚动时间戳 当前时间戳
    返回 假

  定 当前时间 = 格式化时间 "%Y%m%d%H%M%S"
  定 当前日期 = 取子串 当前时间 1 8
  定 当前小时 = 取子串 当前时间 9 2
  定 当前分钟 = 取子串 当前时间 11 2
  定 上次时间 = 格式化时间 "%Y%m%d%H%M%S" 上次滚动时间戳
  定 上次日期 = 取子串 上次时间 1 8
  定 上次小时 = 取子串 上次时间 9 2
  当 滚动时机 等于 "每天"：
    当 当前日期 大于 上次日期：
      设 上次滚动时间戳 当前时间戳
      返回 真


  当 滚动时机 等于 "每小时"：
    当 当前日期 大于 上次日期 或者 (当前日期 等于 上次日期 并且 当前小时 大于 上次小时)：
      设 上次滚动时间戳 当前时间戳
      返回 真


  当 滚动时机 等于 "每分钟"：
    当 当前日期 大于 上次日期 或者 (当前日期 等于 上次日期 并且 当前小时 大于 上次小时) 或者 (当前日期 等于 上次日期 并且 当前小时 等于 上次小时 并且 当前分钟 大于 (取子串 上次时间 11 2))：
      设 上次滚动时间戳 当前时间戳
      返回 真


  返回 假


定 需要按大小滚动 = 函：
  当 日志文件 等于 空：
    返回 假

  当 存在文件 日志文件 不等于 真：
    返回 假

  定 文件大小 = 获取文件字节数 日志文件
  返回 文件大小 大于 最大文件大小


定 执行滚动 = 函：
  当 日志文件 等于 空：
    返回

  当 存在文件 日志文件 不等于 真：
    返回

  定 时间戳 = 格式化时间 "%Y%m%d_%H%M%S"
  定 备份文件 = 日志文件 "。" 时间戳 "。bak"
  重命名文件 日志文件 备份文件
  当 保留备份数 大于 0：
    定 备份列表 = 获取备份文件列表 日志文件
    当 (长度 备份列表) 大于等于 保留备份数：
      定 待删除 = 取 备份列表 0
      删除文件 待删除


  设 上次滚动时间戳 当前时间戳


定 写日志 = 函 级别 消息：
  当 级别 小于 日志级别：
    返回

  当 日志文件 不等于 空：
    当 或者 需要按大小滚动 滚动时机到了：
      执行滚动


  定 格式化消息 = 格式化日志 级别 消息
  印 格式化消息
  当 日志文件 不等于 空：
    追加文件 日志文件 连 格式化消息 "\n"



定 debug = 函 消息：
  写日志 DEBUG 消息


定 info = 函 消息：
  写日志 INFO 消息


定 warning = 函 消息：
  写日志 WARNING 消息


定 error = 函 消息：
  写日志 ERROR 消息


定 critical = 函 消息：
  写日志 CRITICAL 消息

