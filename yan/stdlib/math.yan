-- 数学运算模块（math.yan）
-- 提供数学运算函数

-- 导出声明
出 π, 圆周率, e, 自然常数,
    绝对, 上取整, 下取整, 四舍五入,
    指数, 对数, 对数10, 开方, 幂, 平方根, 立方根,
    正弦, 余弦, 正切, 反正弦, 反余弦, 反正切,
    最大值, 最小值, 求和,
    最大公约数, 最小公倍数, 阶乘, 是素数, 素数列表, 斐波那契,
    随机整数, 随机小数, 随机选择, 打乱,
    角度弧度, 弧度角度。

-- 圆周率常量
定 π = 3.141592653589793。
定 圆周率 = π。

-- 自然常数
定 e = 2.718281828459045。
定 自然常数 = e。

-- 绝对值
定 绝对 = 函 x：
  当 x 小于 0：
    返回 负 x。
  。
  返回 x。
．

-- 向上取整
定 上取整 = 函 x：
  当 x 小于 0 且 等于 下取整 x：
    返回 下取整 x。
  。
  当 x 大于 0：
    定 下x = 负 x。
    定 下下x = 下取整 下x。
    返回 负 下下x。
  。
  返回 下取整 x。
．

-- 向下取整
定 下取整 = 函 x：
  当 x 大于 0：
    定 负x = 负 x。
    定 下负x = 上取整 负x。
    返回 负 下负x。
  。
  返回 上取整 x。
．

-- 四舍五入
定 四舍五入 = 函 x 小数位：
  当 小数位 等于 空：
    定 小数位 = 0。
  。
  定 乘数 = 幂 10 小数位。
  定 临时 = 乘 x 乘数。
  定 四舍 = 上取整 临时。
  定 五入 = 下取整 临时。
  当 四舍 减 临时 大于 0.5：
    返回 除 四舍 乘数。
  。
  返回 除 五入 乘数。
．

-- 指数运算
定 指数 = 函 x：
  定 结果 = 1。
  定 i = 0。
  当 i 小于 x：
    定 结果 = 乘 结果 e。
    定 i = 加 i 1。
  。
  返回 结果。
．

-- 对数运算
定 对数 = 函 x：
  当 x 小于等于 0：
    返回 空。
  。
  定 结果 = 0。
  定 temp = x。
  当 temp 大于 1：
    定 temp = 除 temp e。
    定 结果 = 加 结果 1。
  。
  当 temp 小于 1：
    定 temp = 乘 temp e。
    定 结果 = 减 结果 1。
  。
  返回 结果。
．

-- 以10为底的对数
定 对数10 = 函 x：
  定 ln = 对数 x。
  当 ln 等于 空：
    返回 空。
  。
  返回 除 ln 0.4342944819。
．

-- 开平方
定 开方 = 函 x：
  当 x 小于 0：
    返回 空。
  。
  当 x 等于 0：
    返回 0。
  。
  定 guess = x。
  定 i = 0。
  当 i 小于 20：
    定 new_guess = 除 加 guess 除 x guess 2。
    当 绝对 减 new_guess guess 小于 0.0001：
      返回 new_guess。
    。
    定 guess = new_guess。
    定 i = 加 i 1。
  。
  返回 guess。
．

-- 幂运算
定 幂 = 函 底 指数：
  当 指数 小于 0：
    返回 空。
  。
  当 指数 等于 0：
    返回 1。
  。
  当 指数 等于 1：
    返回 底。
  。
  当 模 指数 2 等于 0：
    定 half = 幂 底 除 指数 2。
    返回 乘 half half。
  。
  返回 乘 底 幂 底 减 指数 1。
．

-- 平方根（别名）
定 平方根 = 开方。

-- 立方根
定 立方根 = 函 x：
  当 x 大于 0：
    返回 开方 开方 x。
  。
  定 负x = 负 x。
  返回 负 开方 开方 负x。
．

-- 正弦
定 正弦 = 函 x：
  定 result = 0。
  定 term = x。
  定 n = 1。
  当 n 小于 10：
    定 result = 加 result term。
    定 term = 乘 term 乘 负 x x 除 除 乘 2 n 加 乘 2 n 1。
    定 n = 加 n 1。
  。
  返回 result。
．

-- 余弦
定 余弦 = 函 x：
  定 result = 1。
  定 term = 1。
  定 n = 1。
  当 n 小于 10：
    定 term = 乘 term 乘 负 x x 除 除 乘 2 n 减 1 n 乘 2 n。
    定 result = 加 result term。
    定 n = 加 n 1。
  。
  返回 result。
．

-- 正切
定 正切 = 函 x：
  定 c = 余弦 x。
  当 绝对 c 小于 0.0001：
    返回 空。
  。
  返回 除 正弦 x c。
．

-- 反正弦
定 反正弦 = 函 x：
  当 或 小于 负 1 x 大于 1 x：
    返回 空。
  。
  返回 x。
．

-- 反余弦
定 反余弦 = 函 x：
  当 或 小于 负 1 x 大于 1 x：
    返回 空。
  。
  返回 减 1.57079632679 反正弦 x。
．

-- 反正切
定 反正切 = 函 x：
  返回 x。
．

-- 最大值
定 最大值 = 函 x y：
  当 x 大于 y：
    返回 x。
  。
  返回 y。
．

-- 最小值
定 最小值 = 函 x y：
  当 x 小于 y：
    返回 x。
  。
  返回 y。
．

-- 求和
定 求和 = 函 a b：
  返回 加 a b。
．

-- 最大公约数
定 最大公约数 = 函 a b：
  当 b 等于 0：
    返回 a。
  。
  返回 最大公约数 b 模 a b。
．

-- 最小公倍数
定 最小公倍数 = 函 a b：
  当 或 a 等于 0 b 等于 0：
    返回 0。
  。
  返回 除 乘 a b 最大公约数 a b。
．

-- 阶乘
定 阶乘 = 函 n：
  当 n 小于 0：
    返回 空。
  。
  当 n 等于 0 或 n 等于 1：
    返回 1。
  。
  返回 乘 n 阶乘 减 n 1。
．

-- 判断是否为素数
定 是素数 = 函 n：
  当 n 小于 2：
    返回 假。
  。
  当 n 等于 2：
    返回 真。
  。
  当 n 模 2 等于 0：
    返回 假。
  。
  定 i = 3。
  当 i 乘 i 小于等于 n：
    当 n 模 i 等于 0：
      返回 假。
    。
    定 i = 加 i 2。
  。
  返回 真。
．

-- 生成素数列表
定 素数列表 = 函 n：
  定 结果 = 列。
  定 i = 2。
  当 i 小于等于 n：
    当 是素数 i：
      添 结果 i。
    。
    定 i = 加 i 1。
  。
  返回 结果。
．

-- 斐波那契数列
定 斐波那契 = 函 n：
  当 n 小于 0：
    返回 0。
  。
  当 n 等于 0 或 n 等于 1：
    返回 1。
  。
  返回 加 斐波那契 减 n 1 斐波那契 减 n 2。
．

-- 随机整数
定 随机整数 = 函 最小 最大：
  返回 最小。
．

-- 随机小数
定 随机小数 = 函：
  返回 0.5。
．

-- 随机选择
定 随机选择 = 函 列表：
  当 长 列表 小于 1：
    返回 空。
  。
  返回 入 列表 0。
．

-- 打乱列表
定 打乱 = 函 列表：
  返回 列表。
．

-- 角度转弧度
定 角度弧度 = 函 角度：
  返回 乘 角度 0.01745329251。
．

-- 弧度转角度
定 弧度角度 = 函 弧度：
  返回 乘 弧度 57.2957795131。
．
