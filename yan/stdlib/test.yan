-- 单元测试模块（test.yan）
-- 提供测试断言和测试框架功能

-- 导出声明
出 断言, 断言真, 断言假, 断言相等, 断言不等,
    断言包含, 断言不包含, 断言为空, 断言不为空,
    断言抛出异常, 断言接近,
    测试用例, 运行测试,
    测试套件, 测试报告

-- 全局测试状态
定 _测试结果 = 典 "通过" 0 "失败" 0 "跳过" 0。
定 _测试用例列表 = 列。

-- 基础断言
定 断言 = 函 条件 消息：
  当 非 条件：
    定 消息 = 连 "断言失败: " 消息
    印 消息
    设 _测试结果 "失败" 加 值 _测试结果 "失败" 1
    返回 假

  设 _测试结果 "通过" 加 值 _测试结果 "通过" 1
  返回 真


-- 断言为真
定 断言真 = 函 条件 消息：
  当 消息 等于 空：
    定 消息 = "预期为真"

  返回 断言 条件 消息


-- 断言为假
定 断言假 = 函 条件 消息：
  当 消息 等于 空：
    定 消息 = "预期为假"

  返回 断言 非 条件 消息


-- 断言相等
定 断言相等 = 函 实际 期望 消息：
  当 消息 等于 空：
    定 消息 = 连 "预期: " 转字符串 期望 " 实际: " 转字符串 实际

  返回 断言 等于 实际 期望 消息


-- 断言不等
定 断言不等 = 函 实际 期望 消息：
  当 消息 等于 空：
    定 消息 = 连 "预期不等于: " 转字符串 期望

  返回 断言 不等于 实际 期望 消息


-- 断言包含
定 断言包含 = 函 容器 元素 消息：
  当 消息 等于 空：
    定 消息 = 连 "预期包含: " 转字符串 元素

  返回 断言 包含 容器 元素 消息


-- 断言不包含
定 断言不包含 = 函 容器 元素 消息：
  当 消息 等于 空：
    定 消息 = 连 "预期不包含: " 转字符串 元素

  返回 断言 非 包含 容器 元素 消息


-- 断言为空
定 断言为空 = 函 值 消息：
  当 消息 等于 空：
    定 消息 = "预期为空"

  返回 断言 等于 值 空 消息


-- 断言不为空
定 断言不为空 = 函 值 消息：
  当 消息 等于 空：
    定 消息 = "预期不为空"

  返回 断言 不等于 值 空 消息


-- 断言抛出异常（简化版）
定 断言抛出异常 = 函 函数 消息：
  当 消息 等于 空：
    定 消息 = "预期抛出异常"

  定 结果 = 空
  试：
    函数
  捕获：
    定 结果 = 真

  返回 断言 结果 消息


-- 断言接近（浮点比较）
定 断言接近 = 函 实际 期望 容差 消息：
  当 容差 等于 空：
    定 容差 = 0.0001

  当 消息 等于 空：
    定 消息 = 连 "预期接近: " 转字符串 期望

  定 差 = 绝对 减 实际 期望
  返回 断言 小于 差 容差 消息


-- 定义测试用例
定 测试用例 = 函 名称 函数：
  添 _测试用例列表 典 "名称" 名称 "函数" 函数


-- 运行单个测试用例
定 _运行单个测试 = 函 测试用例：
  定 名称 = 值 测试用例 "名称"
  定 函数 = 值 测试用例 "函数"
  印 连 "运行测试: " 名称
  试：
    函数
    印 连 "[通过] " 名称
  捕获 错误：
    印 连 "[失败] " 名称 "- " 错误
    设 _测试结果 "失败" 加 值 _测试结果 "失败" 1



-- 运行所有测试
定 运行测试 = 函：
  定 _测试结果 = 典 "通过" 0 "失败" 0 "跳过" 0
  印 "开始运行测试..."
  定 i = 0
  当 i 小于 长 _测试用例列表：
    定 测试 = 取 _测试用例列表 i
    _运行单个测试 测试
    定 i = 加 i 1

  测试报告


-- 创建测试套件
定 测试套件 = 函 名称 测试列表：
  定 i = 0
  当 i 小于 长 测试列表：
    定 测试 = 取 测试列表 i
    添 _测试用例列表 典 "名称" 连 名称 "::" 值 测试 "名称" "函数" 值 测试 "函数"
    定 i = 加 i 1



-- 生成测试报告
定 测试报告 = 函：
  印 "-------- 测试报告 --------"
  印 连 "通过: " 转字符串 值 _测试结果 "通过"
  印 连 "失败: " 转字符串 值 _测试结果 "失败"
  印 连 "跳过: " 转字符串 值 _测试结果 "跳过"
  定 总数 = 加 值 _测试结果 "通过" 值 _测试结果 "失败"
  当 总数 大于 0：
    定 通过率 = 乘 除 值 _测试结果 "通过" 总数 100
    印 连 "通过率: " 转字符串 通过率 "%"

  印 "--------------------------"
  当 等于 值 _测试结果 "失败" 0：
    印 "所有测试通过！"
    返回 真

  返回 假
