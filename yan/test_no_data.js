// Test without Data section
const wasm = new Uint8Array([
    0x00, 0x61, 0x73, 0x6d, 0x01, 0x00, 0x00, 0x00,
    0x01, 0x04, 0x01, 0x60, 0x00, 0x00,
    0x03, 0x02, 0x01, 0x00,
    0x05, 0x04, 0x03, 0x01, 0x00, 0x01,
    0x07, 0x08, 0x01, 0x04, 0x6d, 0x61, 0x69, 0x6e, 0x00, 0x00,
    0x0a, 0x04, 0x01, 0x02, 0x00, 0x0b,
]);

console.log('Without Data:');
console.log('  Length:', wasm.length);
console.log('  Hex:', wasm.toString('hex'));
console.log('  Valid:', WebAssembly.validate(wasm) ? 'Valid' : 'Invalid');
if (!WebAssembly.validate(wasm)) {
    WebAssembly.compile(wasm).catch(e => console.log('  Error:', e.message.split('\n')[0]));
} else {
    WebAssembly.compile(wasm).then(m => {
        console.log('  Compile OK');
        const inst = new WebAssembly.Instance(m);
        console.log('  Instance OK');
        console.log('  Exports:', Object.keys(inst.exports));
    });
}

// Add Data section properly
const wasmWithData = new Uint8Array([
    0x00, 0x61, 0x73, 0x6d, 0x01, 0x00, 0x00, 0x00,
    0x01, 0x04, 0x01, 0x60, 0x00, 0x00,
    0x03, 0x02, 0x01, 0x00,
    0x05, 0x04, 0x03, 0x01, 0x00, 0x01,
    0x07, 0x08, 0x01, 0x04, 0x6d, 0x61, 0x69, 0x6e, 0x00, 0x00,
    0x0a, 0x04, 0x01, 0x02, 0x00, 0x0b,
    0x0b, 0x08, 0x01, 0x41, 0x00, 0x0b, 0x03, 0x66, 0x6f, 0x6f
]);

console.log('With Data:');
console.log('  Length:', wasmWithData.length);
console.log('  Hex:', wasmWithData.toString('hex'));
console.log('  Valid:', WebAssembly.validate(wasmWithData) ? 'Valid' : 'Invalid');
if (!WebAssembly.validate(wasmWithData)) {
    WebAssembly.compile(wasmWithData).catch(e => console.log('  Error:', e.message.split('\n')[0]));
} else {
    WebAssembly.compile(wasmWithData).then(m => {
        console.log('  Compile OK');
    });
}