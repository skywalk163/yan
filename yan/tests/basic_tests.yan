套 "算术运算测试"：
  测 "加法运算"：
    断言等 相加 1 2 3

  测 "减法运算"：
    断言等 相减 5 3 2

  测 "乘法运算"：
    断言等 相乘 3 4 12

  测 "除法运算"：
    断言等 相除 10 2 5

套 "数据操作测试"：
  测 "数据创建"：
    定义 数据 = 列 1 2 3
    断言等 长 数据 3

  测 "首个元素"：
    定义 数据 = 列 1 2 3
    断言等 首个 数据 1

  测 "其余元素"：
    定义 数据 = 列 1 2 3
    断言等 其余 数据 列 2 3

套 "高阶函数测试"：
  测 "映射函数"：
    定义 数据 = 列 1 2 3
    定义 平方 = 函数 x 相乘 x x
    定义 值 = 皆 平方 数据
    断言等 值 列 1 4 9

  测 "过滤函数"：
    定义 数据 = 列 1 2 3 4 5
    定义 是偶数 = 函数 x 等 取余 x 2 0
    定义 值 = 只 是偶数 数据
    断言等 值 列 2 4

  测 "归约函数"：
    定义 数据 = 列 1 2 3 4 5
    定义 值 = 归 相加 0 数据
    断言等 值 15

套 "条件语句测试"：
  测 "条件为真"：
    定义 值 = 如果 大 3 2 那么 1 否则 0
    断言等 值 1

  测 "条件为假"：
    定义 值 = 如果 小 3 2 那么 1 否则 0
    断言等 值 0

套 "递归函数测试"：
  测 "阶乘函数"：
    定义 阶乘 = 函数 n
      如果 等 n 1 那么 1 否则 相乘 n 阶乘 相减 n 1
    断言等 阶乘 5 120

  测 "斐波那契数列"：
    定义 斐波 = 函数 n
      如果 小 n 2 那么 n 否则 相加 斐波 相减 n 1 斐波 相减 n 2
    断言等 斐波 10 55
