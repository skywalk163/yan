-- 简单测试文件
-- 验证 yan 编译器能否正确运行

-- 测试基本算术运算
定义 结果1 = 相加 1 2。
定义 结果2 = 相乘 3 4。
定义 结果3 = 相减 10 5。

-- 测试列表操作
定义 列表 = 列 1 2 3 4 5。
定义 长度 = 长 列表。

-- 测试函数数定义义
定义 平方 = 函数 x：相乘 x x。
定义 平方结果 = 平方 5。

-- 打打印结果
打印 "算术测试:"。
打印 结果1。
打印 结果2。
打印 结果3。

打印 "列表测试:"。
打印 列表。
打印 长度。

打印 "函数数测试:"。
打印 平方结果。
